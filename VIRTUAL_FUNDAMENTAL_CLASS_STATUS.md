# Behrend--Fantechi virtual fundamental class

Specification: [Tau Ceti roadmap comparison](https://github.com/TauCetiProject/TauCetiRoadmap/compare/main...Paul-Lez:TauCetiRoadmap:agent/behrend-fantechi-roadmap).

> **Implementation status: incomplete.**  The former conditional absolute and relative APIs have
> been retired because they accepted intrinsic-cone geometry, Chow/Gysin operations, resolved
> cones, and resolution independence as fields.  The public module now exports only completed
> constructions, currently the proper-point comparison.  General Behrend--Fantechi construction,
> independence, functoriality, and relative theory remain absent rather than assumed.

## Layer map

| Layer | Principal modules | Current artifact (not a completion claim) |
| --- | --- | --- |
| 0: spaces and stacks | `Spaces/*`, `Stacks/*`, `Stacks/OverlapSwap.lean`, `Stacks/PointsIndependence.lean`, `Stacks/StackProducts.lean`, `Curves/SmoothPureDimension.lean`, `Stacks/SheafGluing.lean`, `Stacks/SieveCover.lean`, `Stacks/SieveDescentData.lean`, `Stacks/TorsorStack.lean` |  fppf sheaves, fully faithful schemes and algebraic spaces as discrete stacks, representable properties, constructed binary categorical products and arbitrary categorical pullbacks of algebraic spaces, a constructed algebraic/Deligne--Mumford stack structure on every algebraic space, groupoid fibres, strong-morphism scheme charts, action-torsor groupoids together with their constructed base change along an arbitrary scheme morphism (the underlying fppf sheaf is the actual fibre product, and the group action, the equivariant map to `U`, the principal map, and the fppf-local section — whose cover is the base change of the original cover — are all constructed rather than assumed), which is proved functorial and equipped with identity and composition comparison isomorphisms, the unit and associativity coherence laws, and hence an assembled contravariant pseudofunctor `ActionTorsor.pullbackPseudofunctor` from the locally discrete bicategory of schemes into `Cat` whose fibres are exactly the torsor groupoids, now exported as the quotient prestack `quotientPrestack G U = [U/G]` and the classifying prestack `classifyingPrestack G = BG` (the trivial action on the one-point algebraic space), together with the constructed trivial torsor `G × T` over every scheme, whose left-multiplication action, principal isomorphism and global section are all built so that every fibre of `BG` is nonempty, the proved equivalence of each `BG` fibre with the groupoid `FppfTorsor G T` of fppf `G`-torsors, which commutes strictly with base change, and a genuine strong transformation `ActionTorsor.mapTargetStrongTrans` induced by every equivariant map of `G`-spaces, with all three strong-transformation coherence laws proved, in particular the structure morphism `[U/G] ⟶ BG`; the fppf descent condition of these prestacks, namely descent of arrows and effectivity of descent, remains open, so they are not bundled as `FppfStack`, algebraic/DM stack data, functorial transport of descent data along arbitrary strong transformations (including explicit vertical-composition coherence), a concrete groupoid-valued fibrewise two-pullback pseudofunctor with constructed strong projections and invertible comparison, a proved equivalence between its descent-data category and the genuine two-pullback of the projected descent-data categories, a proved comparison of the canonical global-to-descent functor with the induced pullback of the three component equivalences, and therefore a constructed general fibrewise two-pullback `FppfStack`; the canonical presentation is now equipped with a constructed full bicategorical bilimit proof, including explicit cone lifts, comparison-face coherence, uniqueness isomorphisms, and reconstruction of modifications from compatible projected pairs. Complete scheme presentations now transport across invertible stack 2-cells, stack equivalences are presented by identity scheme maps, representable properties are proved invariant under compatible equivalences of both source and target, any two genuine two-pullback presentations are proved equivalent from their bilimit properties, and multiplicative representable properties are stable under every genuine two-pullback projection with both universal-property uniqueness laws retained. Representable properties now also satisfy honest descent (`Stacks/PropertiesDescent.lean`): any two actual scheme presentations of the same base change are proved to sit in a genuine cartesian square of schemes (`StackMorphismPresentation.isPullback_baseChangeHom`, derived only from the two universal properties together with newly proved composition and target-change coherence for induced comparisons), so a Mathlib scheme-morphism property is independent of the chosen presentation, descends along every class of covers for which Mathlib supplies `MorphismProperty.DescendsAlong`, is local on the test schemes for every `IsZariskiLocalAtTarget` property, and remains compatible with composition, base change, arbitrary genuine two-pullbacks, and equivalences of source and target. fppf, hence also smooth and etale, descent is available for smooth, etale, unramified, locally-of-finite-type, locally-of-finite-presentation, quasi-compact, finite-type, finite-presentation, surjective, open-immersion and isomorphism morphisms; fpqc descent for quasi-compactness is absent from the pinned Mathlib and is proved here (`quasiCompact_descendsAlong_fpqcCover`), which is what makes the finite-type and finite-presentation cases available. Flat, separated, proper, and the immersion properties currently descend only along open covers of the test schemes, because Mathlib supplies no faithfully flat descent for `IsAffineHom`, `IsClosedImmersion`, or `Flat`. Constructed symmetry and left/right identity two-pullbacks remain available, and common atlas refinements together with their presentation groupoids are now constructed (`Stacks/AtlasRefinement.lean`): for any two stack morphisms `U → X`, `V → X` the genuine two-pullback supplies a refining morphism `U ×_X V → X` with both comparison projections and both comparison 2-cells, and it is a theorem, not input data, that the refinement inherits every multiplicative representable property of the two given morphisms, in particular smooth-surjectivity, so a common refinement of two atlases is again an atlas (`Genuine.refinementHom_hasRepresentableProperty`, `Genuine.refinementHom_smoothSurjective`, `StackTwoPullback.exists_commonRefinement`); the multiplicativity of `Smooth` and of `Smooth ⊓ Surjective`, absent from the pinned Mathlib, is proved here. From a genuine self two-pullback the groupoid `U ×_X U ⇉ U` is constructed with source, target, unit (the relative diagonal), inversion and composition over the constructed stack of composable pairs, each with its projection 2-cells obtained from the bicategorical universal property and with the full comparison-face coherence (`ConeLiftClassifies`) proved for inversion and composition; source and target are proved to inherit the representable properties of the atlas. Over every test scheme the same groupoid is completely explicit: objects are the objects of `U(T)`, arrows are the actual isomorphisms in `X(T)` between their images, all `Groupoid` axioms are proved, the arrows are in bijection with the objects of the fibre of `U ×_X U` and the automorphism group at a point is exactly the automorphism group of its image in `X`, so every stabilizer arrow is retained. Refinement compatibility holds in both registers: a refinement 2-cell induces a morphism of presentation groupoids with its source and target 2-cells and its comparison face (`refinementGroupoidMap`, `refinementGroupoidMap_classifies`), and fibrewise the induced functor is proved full and faithful, conjugation by the refinement 2-cell being a bijection on arrows; both comparison functors out of a common refinement of two atlases are therefore fully faithful. On the topological side the point relation of a chart is proved independent of the chosen self-overlap presentation (`StackChart.pointRelation_eq`), and for the quotient topology built in `Stacks/Geometry.lean` the atlas point relation is proved to be an equivalence relation, reflexivity, symmetry and transitivity being the constructed unit, inversion and composition of the chosen self-overlap, so the generated closure adds nothing and the underlying point space is the honest orbit set, `atlasPointMap x = atlasPointMap y ↔ atlasPointRelation x y`, with the quotient-topology characterisation `IsOpen S ↔ IsOpen (atlasPointMap ⁻¹' S)`. Independence of that space from the chart itself remains open, as do the chart-level `AtlasCommonRefinement`/`AtlasTripleRefinement` witness records (which would need a constructor assembling a strong-transformation modification out of fibrewise chart comparisons), the uniqueness-driven groupoid identities `inverse ∘ inverse ≅ 𝟙` and associativity of composition, and the former quotient-stack and algebraic-stack-product presentation records; the terminal fppf stack is now constructed from the terminal sheaf of types and proved bicategorically terminal (a structural morphism `FppfStack.toTerminal` out of every stack, with its hom-category proved contractible: any two such morphisms are canonically 2-isomorphic and modifications between them are unique), which yields the absolute product `X × X` as a genuine two-pullback over it and hence a constructed diagonal `stackDiagonal : X ⟶ X × X` obtained as the bilimit lift of the identity cone. `Stacks/Inertia.lean` then constructs the inertia stack `inertiaStack X = X ×_{X × X} X` as the canonical genuine self two-pullback of that diagonal, together with its projection `inertiaProjection X : I_X ⟶ X`, and proves, from the two-pullback itself and not by assumption, that over every test scheme `T` the inertia fibre is equivalent to the category `AutObj (StackFiber X T)` of objects of `X(T)` equipped with an automorphism (`inertiaFiberEquivalence`), with the projection identified with the functor forgetting the automorphism. This description is proved invariant under equivalence of stacks (`inertiaFiberEquivalenceOfStackEquivalence`, via the fibre equivalence induced by `StackEquivalenceData` and functoriality of `AutObj`), is proved to collapse for stacks with thin fibres, in particular schemes (`representedStack_inertiaFiberEquivalence`: inertia of a scheme is the scheme), and transports along any identification of the fibre with another category (`inertiaFiberEquivalenceOfFiberEquivalence`), which is the form needed for `[U/G]` once the quotient prestack is proved to be a stack. Because inertia is a genuine base change of the diagonal, the inertia projection inherits every multiplicative representable scheme-morphism property of the diagonal (`inertiaProjection_hasRepresentableProperty`); in particular a Deligne--Mumford, i.e. unramified, diagonal forces `I_X ⟶ X` to be unramified (`inertiaProjection_unramified`), and an étale diagonal forces it to be étale. `DeligneMumfordStack` stores only an étale surjective atlas and therefore has no redundant diagonal field. The Deligne--Mumford diagonal criterion itself is still open in both directions: `DiagonalHasProperty` (isomorphism sheaves) and `StackHom.HasRepresentableProperty` applied to `stackDiagonal` (base changes of a stack morphism) are two unrelated encodings of the same diagonal, and all descent results in `Stacks/PropertiesDescent.lean` are stated for the second, so no comparison exists yet; moreover deducing an unramified diagonal from an étale atlas needs the base change of the diagonal along `U × U ⟶ X × X`, i.e. products of stack morphisms and a pasting calculus for genuine two-pullbacks, neither of which is available, even though the scheme-level inputs are (`StackChart.unramified_of_isEtaleSurjective`, Mathlib's étale cancellation and fppf descent for unramifiedness). The converse direction needs étale slices through a smooth atlas, for which Mathlib v4.33.1 supplies nothing Stack dimension is now public and atlas-free (`Stacks/Dimension.lean`): `AlgebraicStack.dim X` is the supremum in `WithBot (WithTop ℤ)` of the corrected dimensions `dim U − r` of all atlas dimension presentations, so no chart occurs in the value, and `AlgebraicStack.dim_eq_of_independent`, `dim_eq_of_pureStackDimension`, `pureStackDimension_iff_dim_eq` and `dim_eq_bot_iff` compute it from any single presentation once atlas independence is known. Atlas independence is reduced to two named, isolated inputs rather than assumed: `StackDimensionPresentation.CommonRefinement` is the honest witness record for the overlap `U ×_X V` (only scheme-level geometric data; no field records a dimension of `X`), and `correctedDimension_eq_of_commonRefinement` proves `dim U − r_U = dim V − r_V` by computing the pure dimension of the overlap through each of its two smooth projections, whence `stackDimensionIndependent_of_commonRefinement`. Half of the overlap is constructed outright: `StackDimensionPresentation.overlap` is the actual chart pullback presentation chosen from representability of the first atlas, and `overlap_fst_smooth`, `overlap_fst_surjective`, `overlap_fst_relativeDimension` prove that `U ×_X V ⟶ V` is a smooth surjection of pure relative dimension `r_U`. The 2-categorical form of the other half is also proved: because pure relative dimension is not multiplicative (relative dimensions add), the existing base-change theorem does not apply, so composition of representable properties is reproved for three separate `MorphismProperty`s (`StackHom.comp_hasRepresentableProperty_of_comp`), giving `StackHom.twoPullbackSnd_hasPureRelativeDimension` and, via `StackTwoPullback.swap`, `StackHom.twoPullbackFst_hasPureRelativeDimension`: both projections of a genuine two-pullback carry the pure relative dimension of the opposite morphism. Two gaps remain and are named rather than hidden. First, the scheme-level formula `dim(source) = dim(target) + r` for a smooth surjection of pure relative dimension `r` is stated as the proposition `SmoothPureDimensionFormula` and is unproved: the pinned Mathlib has `topologicalKrullDim` and `SmoothOfRelativeDimension` but no dimension formula for flat or smooth morphisms, the missing input being `dim 𝒪_{W,w} = dim 𝒪_{U,q w} + dim 𝒪_{W_{q w},w}` for a flat local homomorphism (Stacks 02JS); only the degenerate cases `schemePureDimension_of_isIso` and `schemePureDimension_of_surjective_etale` are available, the latter proved here from the topological étale descent of `Curves/RelativeDimension.lean`. Second, the scheme-level second projection `U ×_X V ⟶ U` still needs a leg-exchanging comparison of chart pullback presentations for two distinct charts (only `PullbackPresentation.selfSwap` exists), equivalently a bridge between `StackChart.PullbackPresentation` and `StackMorphismPresentation` of the chart morphism; `CommonRefinement.ofOverlap` takes exactly those three missing facts as arguments. Conditional on the smooth formula, the derived identities are proved with a single sign convention: `correctedDimension_of_smoothCover` (`dim X = dim Y + r` for a smooth atlas cover of relative dimension `r`), `correctedDimension_add` (`dim(X × Y) = dim X + dim Y` in the form available before products of algebraic stacks are constructed), and `correctedDimension_quotient`/`AlgebraicStack.dim_quotient` (`dim [U/G] = dim U − dim G`); unconditionally, `FppfStack.pureStackDimension_ofSchemeAlgebraicStack` proves that a scheme pure of dimension `d`, viewed as a stack, has pure stack dimension `d`, generalizing the former `Spec k` computation. The quotient formula is stated for an abstract atlas of relative dimension `dim G` because `[U/G]` is not yet an algebraic stack here, the `AlgebraicQuotientPresentation.algebraicStack` block remaining retired, and applies verbatim once it is. `Stacks/OverlapSwap.lean` closes the second-projection gap of the stack-dimension argument: `StackChart.PullbackPresentation.swap` exchanges the two legs of a chart pullback presentation for two *distinct* charts (the base object being the tautological object of the second chart), with the comparison 2-cell reversed through `StackChart.swapComparison` and all classification and uniqueness fields derived by conjugation (`StackChart.inducedComparison_swap`, `StackChart.classifies_swap_iff`); hence `StackDimensionPresentation.overlap_snd_smooth`, `overlap_snd_surjective` and `overlap_snd_relativeDimension` are theorems, `StackDimensionPresentation.CommonRefinement.overlap A B` is an unconditional common refinement of any two atlas dimension presentations, and atlas independence of the dimension reduces to the scheme-level dimension formula plus non-emptiness of the atlas schemes, which is genuinely necessary (`stackDimensionIndependent_of_smoothPureDimensionFormula`; the local form `OverlapDimensionFormula X` asks for the formula only on the two projections of the overlaps of atlases of `X`, `stackDimensionIndependent_of_overlapDimensionFormula`). That scheme-level formula is now proved in `Curves/SmoothPureDimension.lean` for schemes locally of finite type over a field: `Curves.pureTopologicalDimension_of_flat_of_pureRelativeDimension` shows that for a flat morphism `q : W ⟶ U` locally of finite presentation between schemes locally of finite type over a field, with `U` pure of dimension `a` and every fibre of `q` pure of dimension `r`, the scheme `W` is pure of dimension `a + r`; the upper bound is the fibre-dimension inequality of `Curves/FibreDimension.lean`, and for the lower bound the dimension of the closure of a point is identified with its height in the specialization order (`Curves.topologicalKrullDim_closure_singleton`), the image of the generic point of a component is proved to be the generic point of a component because flat morphisms are generalizing (`Curves.closure_singleton_map_mem_irreducibleComponents`), the point is the generic point of a component of its fibre (`Curves.closure_singleton_asFiber_mem_irreducibleComponents`), and the height of a point is proved to dominate the height of its image through the transcendence degrees of the residue fields (`Curves.height_eq_toENat_trdeg`, `Curves.height_apply_le_height`); whence `Curves.smoothPureDimensionFormulaAt_of_locallyOfFiniteType` and, for an algebraic stack whose atlas schemes are locally of finite type over a field (`Curves.AtlasLocallyOfFiniteTypeOver`), the unconditional atlas independence `Curves.stackDimensionIndependent_of_atlasLocallyOfFiniteTypeOver` and `Curves.AlgebraicStack.dim_eq_of_atlasLocallyOfFiniteTypeOver`. The dimension formula over an arbitrary (universally catenary Noetherian) base remains open. The underlying topological space of an algebraic stack is now proved independent of the atlas (`Stacks/PointsIndependence.lean`): `StackChart.crossRel A B` is the relation between points of the schemes of two charts given by a point of their overlap `A.scheme ×_X B.scheme`, independent of the chosen overlap presentation (`crossRel_iff`), symmetric through the leg exchange of `Stacks/OverlapSwap.lean` (`crossRel_symm`), reflexive through the unit section (`crossRel_refl`) and composable through the scheme of composable pairs (`crossRel_trans`), so for one chart it is an equivalence relation coinciding with `pointRelation` and, for the chosen atlas, with `atlasPointRelation`; `StackChart.pointSpace A` is the quotient with the quotient topology and `StackChart.chartPointHomeo` a homeomorphism `A.pointSpace ≃ₜ B.pointSpace` for any two smooth surjective charts, obtained from the two projections of the overlap, which are open quotient maps on points (`isOpenQuotientMap_of_smooth_surjective`, from `UniversallyOpen.of_flat`) with the same fibres; the comparison is characterised by the cross relation (`chartPointHomeo_apply_eq_iff`), hence unique, the identity on a single chart, inverted by exchanging the charts, and satisfies the cocycle law (`chartPointHomeo_trans`). Consequently `AlgebraicStack.pointSpaceHomeo` identifies `UnderlyingStackPoint X` with the point space of any smooth surjective chart, `AlgebraicStack.atlasToPoint` is the induced surjective open quotient map from that chart, agreeing with `atlasPointMap` on the chosen atlas, and openness, closedness, quasi-compactness and irreducibility of the underlying space may be tested on any atlas (`isOpen_iff_atlasToPoint`, `compactSpace_of_atlas`, `irreducibleSpace_of_atlas`); `FppfStack.underlyingStackPointHomeoScheme` identifies the underlying space of the stack represented by a scheme `Z` with `Z` itself. The local-ring package needed for divisors remains open. `Stacks/StackProducts.lean` supplies the products of stack morphisms that `Stacks/Inertia.lean` listed as missing: `stackProduct X Y` is the canonical genuine two-pullback over `terminalStack` and is definitionally `stackSelfProduct X` when `Y = X`; because the terminal fibres are one-point groupoids, the comparison face of the bilimit property is automatic (`stackProduct_coneLiftClassifies`), giving a one-dimensional universal property (`stackPair`, `stackPair_unique`, `stackProd_ext`, `stackProdMap`, `stackPair_comp_prodMap`, and `stackDiagonal_iso_pair` identifying the constructed diagonal with `⟨1, 1⟩`); the pasting calculus is proved for representable properties (`StackTwoPullback.Genuine.pastedSnd_hasRepresentableProperty` and its variants, `diagonalBaseChange_hasRepresentableProperty`), while the identification of a pasted rectangle with a genuine two-pullback of the composite cospan is still open; for a chart `u : U ⟶ X`, `selfOverlapSquare` proves that the square `U ×_X U → U × U`, `X → X × X` 2-commutes and both comparison morphisms with the canonical base change `X ×_{X×X} (U × U)` are constructed (`selfOverlapToDiagonalBaseChange`, `diagonalBaseChangeToSelfOverlap`), but they are not yet proved mutually inverse, lifts produced by the bilimit property having opaque fibre components; one direction of the Deligne--Mumford criterion is reduced to two named hypotheses in `stackDiagonal_unramified_of_etaleAtlas` (representability of `stackDiagonal` in the `StackMorphismPresentation` encoding, and `StackChart.HasUnramifiedIsom`), the unconditional halves `StackChart.exists_etaleCover_pair` (over an étale surjective chart any pair of objects becomes a pair of chart objects after an étale surjective base change) and `StackHom.hasRepresentablePropertyRaw_of_coverIso` being proved, with `DeligneMumfordStack.stackDiagonal_unramified` and `DeligneMumfordStack.inertiaProjection_unramified` as the packaged conditional statements; the comparison of the two encodings of representability of the diagonal remains the key open step (Round 12.) **Descent and gluing of fppf sheaves over a base** (`Stacks/SheafGluing.lean`). Both purely sheaf-theoretic inputs listed as missing in the module docstring of `Stacks/QuotientStackClassifying.lean` are now available, unconditionally. Descent of arrows is `GromovWitten.SheafGluing.PartialHom.exists_unique_glue` and its fibre-product forms `SieveHomFamily.exists_unique_glue_hom` and `CoverHomFamily.glue_spec`/`exists_unique_glue`: a compatible family of morphisms `P ×_{y S} y T ⟶ Q ×_{y S} y T` over the members of a covering sieve of `S` comes from a unique morphism `P ⟶ Q`, which lies over `y S` when the family does; separatedness alone is `hom_ext_of_sieve` and `hom_ext_of_cover`. Uniqueness of the glued sheaf is `PartialIso.glue`/`exists_iso_over`, and `isIso_of_cover` shows that a morphism over `S` which becomes an isomorphism on a cover is an isomorphism. Effectiveness of descent is proved for a descent datum along a single morphism `f : X ⟶ S` in a category with pullbacks: `GromovWitten.SheafGluing.DescentDatum` records the fibrewise transition maps over `X ×_S X` with the cocycle condition, `gluePresheaf_isSheaf` proves the sheaf axiom for the explicitly constructed glued presheaf (no sheafification, which is unavailable for `Sheaf Scheme.fppfTopology (Type u)`), and `glueSheafFibreEquiv` identifies the base change of `glueSheaf D` along `f` with `P`. What remains missing is effective descent indexed by an arbitrary covering *sieve* rather than a single covering morphism (blocked by a universe issue: sieve-indexed compatible families are large), and the packaging of `S ↦ (sheaves over y S)` as a `Pseudofunctor.IsStack`. **Descent along fppf covering sieves** (`Stacks/SieveCover.lean`). The mismatch between sieve-indexed and family-indexed descent is resolved. `exists_covering_family` shows that every fppf covering sieve of `S` contains a `u`-small jointly surjective family of flat morphisms locally of finite presentation (indexed by the points of `S`), and `exists_covering_morphism` shows that the coproduct of such a family is a single flat, lfp, surjective morphism `∐ X ⟶ S` through which every member factors; this single morphism is *not* in general a member of the sieve, so the reduction of descent data uses the family. Effectiveness of descent along a covering sieve is then proved unconditionally: `GromovWitten.SieveGluing.IsSheafOver` is the elementary form of a sieve-indexed descent datum (a presheaf over `S` satisfying the sheaf condition for the sections whose base lies in the sieve), `gluePresheaf_isSheaf` proves that the explicitly constructed glued presheaf is a sheaf, and `ofFib_bijective` identifies its fibres over every member of the sieve with the given datum, compatibly with restriction (`ofFib_map`). The fppf packaging is `exists_glue_of_sieve`. What remains is the purely bookkeeping translation between this fibrewise formulation and a sieve-indexed family of sheaves with transition isomorphisms (the "total presheaf" construction), and the packaging of `S ↦ (sheaves over y S)` as a `Pseudofunctor.IsStack`. **From sieve-indexed descent data to the glued fppf sheaf** (`Stacks/SieveDescentData.lean`). The bundled form of a descent datum along an fppf covering sieve — a sheaf `sheaf f hf` over each member of the sieve, transition isomorphisms `sheaf h hh' ≅ pullback (prj f hf) (fppfYoneda.map g)` and the cocycle condition — is converted into the fibrewise form used by `Stacks/SieveCover.lean`. `tr_id` shows that the transition isomorphism along an identity is forced to be the canonical one; `resAux_comp` is the cocycle condition and makes the total presheaf `tot W = Σ (c ∈ R over W), fibre of sheaf c` functorial; `isSheafOver_tot` proves its sheaf condition over `R` from the sheaf conditions of the individual `sheaf c`. Feeding this into `SieveCover.exists_glue_of_sieve` gives `exists_glue_fibrewise`, and `exists_glue_of_descentDatum` states the conclusion in sections form: for every member `f : X ⟶ S` of the sieve and every `b : W ⟶ X`, the sections of `sheaf f hf` over `W` lying over `b` biject with the sections of the glued sheaf lying over `b ≫ f`. This is the elementwise content of `sheaf f hf ≅ pullback π (fppfYoneda.map f)`; assembling those bijections into a natural isomorphism with the two compatibility statements (the exact shape of `TorsorStack.SheafDescentInput.effective`) is the one step still missing, and every ingredient for it — naturality of `sectionTot`, `SheafGluing.isIso_of_sections`, `SheafGluing.tautLift` — is already available. `Stacks/TorsorStack.lean` supplies, unconditionally, the fppf-sheaf gluing theory underlying descent for the quotient prestack `[U/G]`: `RelHomFamily.glue`/`glue_spec` glue a compatible family of morphisms `A ×_S X ⟶ B` over the members of a covering sieve, `hom_ext_of_cover` is the matching separatedness, and `isIso_of_cover` deduces that a morphism of fppf sheaves over a base is an isomorphism as soon as it is one after base change to each member of a covering sieve; all are built explicitly from `Presieve.IsSheafFor.amalgamate`, since `Sheaf Scheme.fppfTopology (Type u)` admits no sheafification. On the torsor side it identifies the pseudofunctorial comparison cells of iterated base change with the explicit fibre products (`mapComp'_hom_app_projOuter`, `mapComp'_inv_app_projOuter_id`, `fibre_comp_iso_hom`, `mapComp'_pair_proj`). `Stacks/QuotientStackPullback.lean` was refactored so that `pullbackPseudofunctor` is assembled from named data and coherence declarations (`pullbackPsObj/Map/MapId/MapComp`, `pullbackPs_associator/_leftUnitor/_rightUnitor`). Descent of arrows and effectivity of descent for `[U/G]` remain unproved, and the obstruction is now identified as a Lean kernel-performance one rather than a mathematical gap: instantiating `Pseudofunctor.DescentData.ofObj_hom` or `Pseudofunctor.LocallyDiscreteOpToCat.map_eq_pullHom` — the only two gateways into Mathlib's descent API, both of which embed tactic-generated proofs inside `Pseudofunctor.mapComp'` — at `pullbackPseudofunctor` elaborates in under a second but does not finish kernel type-checking, while the same instantiation at a discrete pseudofunctor over the same site costs 109 ms. `[U/G]` and `BG` therefore remain prestacks, not `FppfStack`s. |
| 1: modules and cones | `Sites/*`, `Modules/Stack.lean`, `Modules/Derived.lean`, `Cones/Affine.lean`, `Cones/Graded.lean`, `Cones/Quotient.lean`, `Cones/Stack.lean`, `Cones/DeformationSpaceGeometry.lean`, `RelativeSpec.lean`, `Cones/NormalConeGlobal.lean`, `Cones/ConeTranslation.lean`, `Cones/NormalConeRegular.lean`, `Cones/Products.lean` | distinct stack sites, canonical regular-function sheaves, constructed ringed-site identity/composition and module pullback adjunctions, actual module sheaves, direct sums and canonical finite-free sheaves, symmetric affine cones, Rees normal cones, the proved canonical closed immersion `Spec(gr_I R) → Spec Sym(I/I²)`, the proof that this canonical map is an isomorphism when the affine ideal comes with a chosen finite regular generating sequence (`Cones/RegularSequence.lean`, on top of the neutral commutative-algebra file `Algebra/QuasiRegular.lean`), concrete quotient groupoids, and a constructed rank-zero vector-bundle stack; the former relative-Spec/Proj and derived-Picard theorem packages are inactive; `Cones/DeformationSpace.lean` now constructs the affine deformation to the normal cone: the extended Rees algebra `R[It, t⁻¹]` is built as the subalgebra of Laurent polynomials whose degree-`n` coefficient lies in `Iⁿ` for `n ≥ 0`, with the deformation parameter `u = t⁻¹` proved a nonzerodivisor, the special fibre `R[It, t⁻¹]/(u)` proved isomorphic to the associated graded ring `gr_I(R)` (the coordinate ring of the affine normal cone), so that the normal cone is a closed subscheme of the deformation space `M° = Spec R[It, t⁻¹]` lying over the origin of the affine line, the localization at `u` proved to be the full Laurent polynomial ring, so that the generic fibre over `𝔾_m` is the trivial family `Spec R × 𝔾_m` embedded as the open subscheme `D(u)`, the fibre over `u = 1` proved isomorphic to `Spec R` itself (evaluation at `t = 1` is surjective with kernel `(u − 1)`, by an explicit geometric-series division inside the extended Rees algebra), and, over a base field `k`, the deformation space proved torsion-free hence flat over `k[u]`, so the structure morphism `M° → 𝔸¹_k` is a flat morphism of schemes; `Cones/DeformationSpaceGeometry.lean` now packages these fibre identifications as genuine cartesian squares of schemes and supplies the remaining geometry: a general pushout criterion for squares of commutative rings whose two right-hand maps are surjective (`isPushout_of_surjective`, proved from the colimit universal property under the kernel condition `ker i ≤ (ker g)·B`) gives `specialFibre_isPullback` and `normalCone_isPullback` (the fibre of `M° → 𝔸¹` over the origin is `Spec gr_I R`, the affine normal cone) and `fibreOne_isPullback`/`fibreOne_isPullback_spec` (the fibre over `u = 1` is `Spec R`), while `genericFibre_isPullback` exhibits the open piece over `𝔾_m ⊆ 𝔸¹` as the honest base change of `𝔾_m → 𝔸¹`, from Mathlib's localization pushout together with `isLocalization_parameter` and `Submonoid.map_powers`; the deformation of the closed subscheme is an actual ring map `subschemeHom : R[It,t⁻¹] → (R/I)[u]` (reduce coefficients modulo `I` and keep the non-positive powers of `t`, built from the coefficientwise base change `mapLaurent` of Laurent polynomials and the additive retraction of the embedding `(R/I)[u] ↪ (R/I)[t,t⁻¹]`, `u ↦ t⁻¹`), proved surjective and hence a closed immersion `subschemeι : Spec(R/I) × 𝔸¹ → M°` (`subschemeι_isClosedImmersion`) compatible with the projections to `𝔸¹` (`subschemeι_comp_toLine`) and meeting the special fibre exactly in the vertex of the normal cone (`origin_comp_subschemeι`, through the transported augmentation `specialFibreAugmentation` and `vertex_comp_normalConeIso`); base change is constructed for an arbitrary `R`-algebra `B` as a comparison map `reesTensorMap : R[It,t⁻¹] ⊗_R B → B[(IB)t,t⁻¹]`, proved surjective unconditionally (`reesTensorMap_surjective`, by span induction on the extended ideal) and bijective whenever `B` is flat (`reesTensorEquiv`), injectivity coming from the proved linear isomorphism `R[t,t⁻¹] ⊗_R B ≅ B[t,t⁻¹]` (`laurentTensorEquiv`, via `TensorProduct.finsuppScalarLeft`) together with flatness of the inclusion `R[It,t⁻¹] ⊆ R[t,t⁻¹]`, whence the ring pushout `isPushout_reesBaseChange`, the cartesian square `reesBaseChange_isPullback` and its localization case `reesBaseChange_isPullback_isLocalization`; the tangent action is constructed in the `ConeAction` language of `Cones/Quotient.lean` in the general form `ConeQuotient.bundleTranslationAction d`, the action of `Spec Sym(F)` on `Spec Sym(N)` attached to any linear map `d : N →ₗ F`, with the unit, associativity and conic laws all proved and the translation action of a bundle on itself recovered as the case `d = id` (`bundleTranslationCoaction_id`), and is specialised through the constructed conormal map `AffineNormalCone.conormalMap : I/I² → (R/I) ⊗_R Ω[R⁄k]` to `AffineNormalCone.normalSheafTangentAction`, the action of the restricted tangent bundle `Spec Sym_{R/I}((R/I) ⊗_R Ω[R⁄k])` on the affine normal *sheaf* `Spec Sym_{R/I}(I/I²)`, with the affine action morphism `normalSheafActionMap` and its proved unit law; what remains is the descent of this action along the closed immersion `Spec gr_I R → Spec Sym(I/I²)` to the normal *cone* itself, which requires the translation coaction to preserve the kernel of `Sym_{R/I}(I/I²) ↠ gr_I R` — true when the ambient scheme is smooth over `k` (Vistoli's lemma, the input to the intrinsic normal cone) and unavailable from the pinned Mathlib — together with the globalization of all of this over a non-affine base; `Cones/Graded.lean` now constructs affine graded cones over an affine base as actual data rather than fields: a graded `R`-algebra is encoded by its `𝔸¹`-contraction coaction `ψ : S →ₐ[R] S[t]` (`ψ s = s·tⁿ` in degree `n`), whose counit and coassociativity laws are proved for every cone built in the file, and from which the monoid-action axioms on `B`-points are derived (`scale_one`, `scale_scale`) together with `contraction_one`, `contraction_mul` and, for a vertex `ε`, `contraction 0 = ofId ∘ ε` and the proof that the vertex is a section of the projection and a fixed point of every contraction, also as morphisms of schemes (`vertexSection_comp_projection`, `contractionMap_one/_mul/_zero`, `vertexSection_comp_contractionMap`); the internal grading is recovered from the coaction (`homogeneous ψ n`, degree projections `proj ψ n` with `s = Σ proj n s` from the counit and `proj n s ∈ homogeneous ψ n` from coassociativity), and Mathlib's `GradedAlgebra 𝒮` is bridged to it with `homogeneous (gradedCoaction 𝒮) n = 𝒮 n`, giving polynomial rings graded by total degree as an example; the affine cone `Spec Sym(M)` and the Rees normal cone `Spec gr_I(R)` are proved to be instances: for `Sym(M)` the augmentation is proved to be the vertex and the new contraction is proved equal to the existing `AffineCone.contraction`, and for `gr_I(R)` the coaction is constructed by restricting the comultiplication of `R[t]` to the Rees algebra (proved to land there coefficientwise) and descending along the ideal `I·Rees_I(R)`, proved homogeneous, with the resulting contraction proved equal to the existing `AffineNormalCone.reesScale` and `associatedGradedScale`; morphisms of cones are graded algebra maps intertwining the coactions, proved to commute with all contractions and to carry vertices to vertices (not assumed), closed subcones are constructed as graded quotients by homogeneous ideals (with the closed immersion on spectra), products of cones are constructed as tensor products with the total grading together with the two projections as cone morphisms and the product vertex, and the abelian hull `Spec Sym(S₁) ← Spec S` is constructed and proved to be a closed immersion whenever the cone is generated in degree one; on vector bundles `C(F) = Spec Sym(F)` the functor of points is proved to be `Hom_R(F, B)` with the contraction action corresponding to scalar multiplication of linear maps, the degree-one part of `Sym(F)` is proved to be exactly `F` for an arbitrary module (`ι` is injective via the square-zero extension, and every degree-one element comes from `F`), hence the bundle rank equals the module rank (`rank_homogeneous_one`, `finrank_homogeneous_one`), `C(F)` is proved smooth over the base for `F` finite free, and base change is proved as an algebra isomorphism `Sym_B(B ⊗_R F) ≃ₐ[B] B ⊗_R Sym_R(F)`; the comparison with the rank-zero vector-bundle stack of `Cones/Stack.lean`, the `R/I`-linear vertex of the normal cone, and smoothness for merely finite projective modules remain to be constructed; `Modules/Derived.lean` additionally constructs the big-étale and big-fppf ringed sites of schemes (both ringed by the representable regular-function sheaf) and an actual `RingedSiteMorphism` between them, whose site functor is the identity and whose continuity is the proved inclusion `etaleTopology ≤ fppfTopology`; the induced pullback of module sheaves `etaleToFppfModules` is exported together with the identity and composition comparisons obtained from the constructed ringed-site machinery; a ringed-site morphism from the small étale site of a scheme (or of a Deligne–Mumford stack) into the big fppf site remains absent, since it needs continuity of `Scheme.Etale.forget X ⋙ Over.forget X` from `Scheme.smallEtaleTopology X` to `Scheme.fppfTopology`, which Mathlib does not supply; `Cones/Quotient.lean` now constructs, at the affine level, actions of a vector bundle on a cone: an action of `E = Spec Sym(F)` on the affine cone `C = Spec S` over `Spec R` is packaged as `ConeQuotient.ConeAction`, whose data are the `𝔸¹`-contraction coaction of `Cones/Graded.lean` and a coaction `α : S →ₐ[R] S ⊗_R Sym(F)`, and whose `Prop` fields are hypotheses about that data (the unit and associativity laws of the induced action `ConeQuotient.translate` on `B`-points `(S →ₐ[R] B) × (F →ₗ[R] B) → (S →ₐ[R] B)`, and the requirement `conic` that the action map `C ×_{Spec R} E → C` be a morphism of cones) which are proved, not assumed, for every action built in the file; the Hopf comultiplication `symComul : Sym(F) → Sym(F) ⊗ Sym(F)` is constructed from the diagonal and proved to compute the addition of `B`-points (`lift_comp_symComul`), `symMap` gives the coordinate algebra map of a morphism of bundles, and the action on points is proved natural in the test algebra (`comp_translate`); the two examples are constructed with all axioms proved: the translation action of `E` on itself (`translationAction`, from `symComul`) and the free action of `E` on a product `C ×_{Spec R} E` (`freeAction`, built from `freeLeft`/`freeRight`/`freeCoaction` and the new `isConeHom_tensor_ext` criterion for a map out of a product of cones to be conic); from `conic` it is proved that contractions commute with the action, `scale (translate φ l) b = translate (scale φ b) (b • l)` (`ConeAction.scale_translate`), so the action is `𝔾ₘ`-equivariant, and that contracting any point by `0` is the vertex and the vertex is fixed by every contraction over every test algebra (`scale_zero_eq`, `scale_vertex`); on spectra the action and the zero section are constructed as morphisms of affine schemes (`actionMap`, `zeroSectionMap`) with the unit law `zeroSectionMap_comp_actionMap` and the equivariance `actionMap_comp_contractionMap` proved; equivariant morphisms of cones-with-actions are `IsEquivariant A A' φ u` (a `Prop` about `φ : S →ₐ[R] S'` and `u : F →ₗ[R] F'`, not stored data), proved closed under identity and composition and proved to intertwine the point actions (`translate_comp`); still missing at this level are products and fibre products of cone actions, which need the canonical isomorphism `Sym(F ⊕ F') ≃ Sym(F) ⊗ Sym(F')` that Mathlib's `SymmetricAlgebra` API does not provide (only `equivMvPolynomial` for a module with a basis), and any non-affine base; `RelativeSpec.lean` now constructs relative `Spec` over an arbitrary scheme: a quasi-coherent algebra is given affine-locally (`AlgebraData`: an algebra over the section ring of every affine open, functorial transition maps, and the requirement that each transition square be an actual pushout of commutative rings, which `isPushout_of_algEquiv` derives from a bijective base-change map `Γ(U) ⊗ 𝒜(V) → 𝒜(U)`), and `relativeSpec X 𝒜` is glued from the affine spectra by Mathlib's relative gluing along the locally directed cover by affine opens, with the affine structure morphism `toBase` (proved affine, `toBase_isAffineHom`, and an isomorphism when all structure maps are bijective), cartesian affine pieces (`isPullback_affine`), and the affine pieces covering the total space; a morphism of algebra data (`RelativeSpec.Hom`, a compatible family of algebra maps) induces a morphism of relative spectra over `X` (`Hom.map`, `map_toBase`) whose restriction to every affine piece is the affine `Spec.map` (`isPullback_map`), so that any property local on the target holds for it as soon as it holds affine-locally (`property_map_of_forall`); in particular a surjective family gives a closed immersion and a bijective family an isomorphism. `Cones/NormalConeGlobal.lean` applies this to a quasi-coherent ideal sheaf `I` on any scheme `X`: the normal cone `C_{Z/X} = Spec_X (gr_I 𝒪_X)` (`NormalCone.normalCone`) and the normal sheaf `N_{Z/X} = Spec_X Sym(I/I²)` (`NormalSheaf.normalSheaf`) are relative spectra of the affine `gr_{I(U)}` and `Sym(I(U))/I(U)·Sym(I(U))`, with both structure morphisms affine and both proved to restrict to the affine constructions of `Cones/Affine.lean` over every affine open; the required pushout squares along the restriction maps of affine opens are proved, not assumed, from flatness of those restriction maps: the Rees algebra commutes with flat base change (`reesTensorEquiv`, from `ReesBlowup.reesBaseChangeMap`), an ideal commutes with flat base change (`idealTensorEquiv`, `B ⊗ I ≅ IB`), the symmetric algebra commutes with arbitrary base change (`symTensorEquiv`, via `GradedCone.baseChangeEquiv`), and quotients by extended ideals commute with arbitrary base change (`quotBaseChangeEquiv`, from Mathlib's `Algebra.TensorProduct.tensorQuotientEquiv`), giving `grTensorEquiv` and `nsTensorEquiv`; the canonical surjection `Sym(I) → Rees(I)` onto the degree-one generators (`symToRees`, surjective by `adjoin_monomial_eq_reesAlgebra`) descends to the compatible family of surjections `nsToGr : Sym(I/I²) → gr_I` and hence to a closed immersion `NormalCone.toNormalSheaf : C_{Z/X} → N_{Z/X}` over `X` (`toNormalSheaf_isClosedImmersion`, `toNormalSheaf_toBase`), which over every affine open is proved to be the affine `AffineNormalCone.coneToNormalSheaf` of `Cones/Affine.lean` composed with the identification `normalSheafRingEquiv : Sym(I)/I·Sym(I) ≃ Sym_{A/I}(I/I²)` (`isPullback_coneToNormalSheaf`); still missing are flat base change and product formulas for the glued cones and relative `Spec` on stacks. `Cones/NormalConeRegular.lean` supplies three of the items previously listed as missing for the glued normal cone: the affine regular-immersion theorem is generalised to quasi-regular generating sequences (`AffineNormalCone.QuasiregularGenerators`, the hypothesis that `Cones/RegularSequence.lean` actually uses), the resulting bijectivity of the gluing comparison `nsToGr : Sym(I)/I·Sym(I) → gr_I` is transported through `normalSheafRingEquiv`, and `RelativeSpec.Hom.isIso_map` gives `NormalCone.isIso_toNormalSheaf`: for a quasi-coherent ideal sheaf which on every affine open is generated by a quasi-regular (in particular a regular) sequence, the canonical closed immersion `C_{Z/X} → N_{Z/X}` is an isomorphism over `X` (`NormalCone.isoNormalSheaf`); the comparison is proved to be local on the base: bijectivity of `nsToGr` is inherited by smaller affine opens through flat base change (`NormalCone.bijective_nsToGr_of_le`) and descends along an affine open cover (`NormalCone.bijective_nsToGr_of_cover`, by the localisation argument `exists_pow_smul_eq_zero`, `eq_zero_of_forall_exists_pow_smul_eq_zero` with the sections whose basic opens lie in members of the cover spanning the unit ideal), so `NormalCone.isIso_toNormalSheaf_of_cover` and `NormalCone.isIso_toNormalSheaf_of_forall_mem_cover`/`_of_regular_cover` need the quasi-regular (in particular regular) generators only on the members of one affine open cover, the usual definition of a regular immersion. The abelian-hull statement is proved: the degree-one part of the cone `Spec gr_I(R)` over its own base `Spec(R/I)` is exactly the conormal module (`homogeneous_one_eq_range`, `conormalToAssociatedGraded` injective), under this identification `Sym_{R/I}(I/I²) → gr_I(R)` is the abelian hull map of `Cones/Graded.lean` (`abelianHull_symCongr`, `isGeneratedInDegreeOne_grQuotCoaction`), and over every affine open the global `toNormalSheaf` is the abelian hull (`NormalCone.isPullback_abelianHull`). The structure morphisms are factored through the closed subscheme: `RelativeSpec.toSubscheme` maps a relative `Spec` to `I.subscheme` whenever the chart structure maps are killed by `I`, giving `NormalCone.toSubscheme` and `NormalSheaf.toSubscheme` with `toSubscheme ≫ I.subschemeι = toBase` and `toNormalSheaf ≫ NormalSheaf.toSubscheme = NormalCone.toSubscheme`. Products and fibre products of affine cones and of cone actions are now available over an affine base (`Cones/Products.lean`): the universal property of the product cone (`GradedCone.isConeHom_tensorLift`, `GradedCone.tensorLift_unique`, `GradedCone.isConeHom_tensorMap`) and the fibre-product square `GradedCone.isPullback_projection` for `Spec (S ⊗[R] S')`; using `NormalSheafPicard.symProdEquiv`, `GradedCone.isConeHom_symProdEquiv` and its inverse show that `Sym(M × N) ≃ Sym M ⊗ Sym N` is an isomorphism of cones and `GradedCone.isPullback_symProd` identifies `C(M × N)` with `C(M) ×_{Spec R} C(N)`, the projections being induced by `LinearMap.inl`/`LinearMap.inr`; for cone actions, `ConeQuotient.ConeAction.prod` constructs the action of `E × E'` on `C ×_{Spec R} C'`, `ConeQuotient.ConeAction.pullback` restricts an action along a morphism of vector bundles, and `ConeQuotient.ConeAction.diagonal` gives the diagonal action of a single `E` on a fibre product, with the unit, associativity and conic laws proved and all projections equivariant. The vector-bundle criterion is unconditional for finite projective modules: `GradedCone.smooth_symmetricAlgebra_of_projective` proves `Algebra.Smooth R (Sym F)` and `GradedCone.smooth_projection_of_projective` the smoothness of `C(F) → Spec R`, via the new general lemmas that a retract of a smooth (resp. finitely presented, formally smooth) algebra is again one (`AlgebraRetract.smooth`, `AlgebraRetract.finitePresentation`, `AlgebraRetract.formallySmooth`, with the kernel computation `AlgebraRetract.ker_eq_span`); the converse `GradedCone.projective_of_flat_symmetricAlgebra` deduces projectivity of a finitely presented `F` from flatness of `Sym F`. The `R/I`-linear vertex of the normal cone is constructed (`GradedCone.normalConeCoactionQuot`, `GradedCone.normalConeVertexHom`, `GradedCone.isConeVertex_normalConeCoactionQuot`). Still missing at this level: any non-affine base for products, the comparison of `Spec Sym F` with the `VectorBundleStack`/`stackRank` of `Cones/Stack.lean` |
| 2: rational intersection theory | `IntersectionTheory/*` | scheme cycles graded by actual closure dimension, functorial proper pushforward on those cycle groups, constructed orders of vanishing, canonical rational equivalence, closed-immersion Chow pushforward, the proper-point Chow computation, and a constructed flat pullback along arbitrary open immersions which is proved rational-linear and contravariantly functorial, proved to descend to the rational Chow quotient, and assembled into the right-exact part of the localization sequence: restriction to an open subscheme of a Noetherian scheme is proved surjective through an actual extension-by-zero section, and it is proved to annihilate every class pushed forward from a closed subscheme of the complement; the principal divisor of a rational function is proved to have zero Chow class. `IntersectionTheory/StackChowVistoli.lean` now adds the Vistoli descent construction on top of this: for a scheme-level presentation groupoid `R ⇉ U` whose source and target are open immersions of relative dimension zero (`OpenPresentationGroupoid`, a data-only record with no conclusion stored in a field), the Vistoli cycles are constructed as the equalizer `{α ∈ Z_i(U) : s^*α = t^*α}` of the two genuine flat pullbacks, proved equal to the pointwise descent condition `α(s(r)) = α(t(r))` (`mem_cycles_iff`), and the Vistoli rational Chow group `chow` is their quotient by rational equivalence on the atlas. A refinement of presentations (an open immersion of atlases intertwined with a map of arrow schemes, the scheme shadow of `refinementGroupoidMap`) is proved, not assumed, to carry Vistoli cycles to Vistoli cycles and to preserve rational equivalence, hence to induce a map of Vistoli Chow groups which is proved functorial (`chowMap_id`, `chowMap_trans`); two presentations with a common refinement are therefore canonically comparable (`commonRefinementComparison`), and an invertible refinement induces an isomorphism (`Refinement.chowEquiv`, `commonRefinementEquiv`). For a presentation whose two legs agree, in particular the identity atlas of a scheme, every cycle is proved to descend and the Vistoli Chow group is proved canonically isomorphic to the scheme rational Chow group, compatibly with both quotient maps (`chowEquivSchemeChow`, `identityPresentationChowEquiv`), so the construction genuinely extends the scheme theory. On the stabilizer side the rational weight `1/n` is constructed with its normalization `n · (1/n) = 1` and the weight-one criterion for trivial stabilizers; for an object of a presentation groupoid the weight is proved to be `1/|Aut|` of its image in the base stack (`autWeight_presentationGroupoid`, via `PresentationGroupoid.autEquiv`) and is proved invariant under refinement of atlases (`autWeight_refinementFunctor`, via the fully faithful refinement functor). The stabilizer-weighted degree of a zero-cycle is constructed and proved independent of the finite set used to compute it, and on the one-point presentation over `Spec k` it is assembled into an actual homomorphism `chow 0 →ₗ[ℚ] ℚ` on the Vistoli Chow group, for which the fundamental class of a point with stabilizer group `Γ` is proved to have degree `1/|Γ|`, with `|Γ| · deg = 1` (`pointDegree_pointFundamentalClass`, `card_mul_pointDegree_pointFundamentalClass`, and `weightedDegree_fundamentalCycle_of_autEquiv` with the stabilizer read off from a genuine presentation-groupoid object). What is still missing is precisely one geometric input: flat pullback of algebraic cycles along a flat morphism of positive relative dimension. Mathlib v4.33.1 supplies only `AlgebraicCycle.map` (proper pushforward) and no `AlgebraicCycle.pullback`, so the source and target of the presentation groupoid of a genuine smooth or étale atlas, smooth surjective and not open immersions, are outside the construction, and `OpenPresentationGroupoid` cannot yet be instantiated on `AlgebraicStack.chosenAtlasSelfOverlap`. Consequently atlas independence is proved only in the refinement form above: that the comparison map of an arbitrary smooth refinement is an isomorphism is exactly exactness of `0 → A(X) → A(U) ⇉ A(R)` for a smooth cover and needs both that flat pullback and its surjectivity (Fulton, Intersection Theory, Prop. 1.9 with descent), neither of which exists. `BΓ` is likewise unavailable as a Deligne--Mumford stack, since `classifyingPrestack` is only a prestack until fppf descent for action torsors is proved, so the degree theorem is stated for the one-point atlas with stabilizer group `Γ` that such a presentation would supply; and the weighted degree descends to Chow classes only where rational equivalence is trivial, general descent being the degree theorem `deg div(f) = 0`, which rests on the same missing norm/`Ring.ord`-additivity input already recorded for general proper pushforward. Stack cycle generators, rational functions, and atlas divisor calculations in `StackChow.lean` remain experimental and their uncertified quotient and all stack pullback, positive-rank Gysin, and Chern operations are inactive; `IntersectionTheory/ChernClasses.lean` now adds the vector-bundle flat pullback with a proved dimension shift: for an affine vector bundle `E = Spec A → Spec R` over a Noetherian ring, trivialized by an `R`-algebra isomorphism `A ≃ₐ[R] MvPolynomial ι R` (the bundle `Spec Sym F` of a finite free module of `Cones/Graded.lean` is such, via `VectorBundle.symTrivialization`), the generic point of the preimage of a subvariety is constructed as the extended prime `p·A` (`VectorBundle.bundlePrime`), proved prime, proved to lie over `p`, proved injective in `p` and proved independent of the trivialization, and its closure dimension is proved to exceed that of the subvariety by the rank (`VectorBundle.coheight_bundlePrime`, from Mathlib's Krull dimension of a polynomial ring over a Noetherian ring applied to `R ⧸ p`); identifying the specialization order on the points of an affine scheme with the dual of the inclusion order on primes (`VectorBundle.specOrderIso`) transports this to the certified `DimensionFunction` gradings (`VectorBundle.dimension_bundlePoint`), so the grading of the resulting map is a theorem and not a hypothesis; the flat pullback of cycles `AlgebraicCycle.pullbackBundle` is constructed with all multiplicities one, proved locally finite, rational-linear and injective, and assembled into the graded map `cyclesOfDimension.flatPullbackBundle : Z_i(X) →ₗ[ℚ] Z_{i+r}(E)` (`symFlatPullback` for `Spec Sym F`, whose rank is proved to be `finrank R F`); invariance under isomorphism of bundles is proved, not assumed: an isomorphism of coordinate algebras induces an isomorphism of total spaces (`specIsoOfAlgEquiv`), the two certified gradings are proved to agree along it (`dimension_comap_algEquiv`), the bundle pullbacks are proved to correspond (`pullbackBundle_algEquiv`, `flatPullbackOpen_flatPullbackBundle`), and the two flat pullbacks along the mutually inverse open immersions are proved mutually inverse, giving an isomorphism of rational Chow groups (`VectorBundle.chowEquivOfAlgEquiv`); on the zero section, the vertex section is proved to be a closed immersion because an `R`-algebra map to `R` is automatically surjective, giving the Chow pushforward `VectorBundle.zeroSectionPushforward : A_i(X) →ₗ[ℚ] A_i(E)`, and for a closed immersion which is also an open immersion, flat pullback is proved to be a retraction of the pushforward (`cyclesOfDimension.flatPullbackOpen_properPushforward`, `openImmersionPullback_pushforward_eq_id`, specialized in `VectorBundle.zeroSectionGysin`), which is the self-intersection formula in the case where the top Chern class is `1`; in rank zero the structure map is proved surjective, every point of the total space is proved to be the generic point of the preimage of its image, the bundle pullback is proved to coincide with the existing open-immersion flat pullback along the projection, the projection and zero section are proved to be mutually inverse isomorphisms, and the shift is `0`, so the positive-rank construction genuinely restricts to the rank-zero identity of `Gysin.lean`; what is still missing is the descent of `flatPullbackBundle` to rational equivalence, and with it homotopy invariance, the positive-rank zero-section Gysin inverse, Chern classes, the projective-bundle formula and Whitney sum; the order-of-vanishing obstruction to this descent is now removed by `IntersectionTheory/BundlePullbackDescent.lean`: `Ring.ord_algebraMap_of_flat_local_of_map_maximalIdeal` and `AlgebraicGeometry.Scheme.ord_flatLocalFunctionFieldMap` prove that a flat local extension with `m_R · S = m_S` preserves the order of vanishing without any unramifiedness (so the transcendental residue extension of the bundle stalk is allowed; the polynomial localisation is instantiated in `VectorBundle.ord_algebraMap_localizationAtPrime_map_C`), `VectorBundle.coheight_bundlePoint` proves that the generic point of a preimage has the same codimension (flatness gives going down), `VectorBundle.ord_bundlePoint` proves the equality of orders at every bundle point, and `VectorBundle.pullbackBundle_principalCycle` proves the principal-divisor comparison `π^* div(f) = div(π^* f)`, hence `VectorBundle.pullbackBundle_principalCycle_mem_totalRationalRelations`, for every generator whose integral closed subscheme is the whole base `Spec R` (`R` a Noetherian domain); `IntersectionTheory/BundlePullbackChow.lean` supplies exactly that presentation and completes the descent: an arbitrary `IntegralClosedSubscheme` of `Spec R` is presented as `Spec (R ⧸ p)` with `p` prime (`IsClosedImmersion.Spec_iff`, `IsIntegral.of_isIso`, `affine_isIntegral_iff`, `Ideal.Quotient.isDomain_iff_prime`), its restricted bundle is the polynomial algebra `MvPolynomial ι (R ⧸ p)` with the surjection `VectorBundle.quotBundleHom : A ↠ MvPolynomial ι (R ⧸ p)` whose kernel is proved to be the extended ideal `p · A` (`VectorBundle.ker_quotBundleHom`), and the resulting closed immersion `E_V ⟶ E` identifies `E_V` with the preimage of `V` on cycle-carrying points by the ideal identity `VectorBundle.comap_quotBundleHom_map_algebraMap` and its geometric form `VectorBundle.quotBundleImmersion_base_bundlePoint`; the compatibility `π^* ∘ V_* = (E_V)_* ∘ π_V^*` of `AlgebraicCycle.pullbackBundle` with residue-degree pushforward along the two closed immersions is proved (`VectorBundle.pullbackBundle_map_quotImmersion`), and combined with `VectorBundle.pullbackBundle_principalCycle` over the Noetherian domain `R ⧸ p` and the transport of a principal cycle along the presentation isomorphism (`map_isIso_principalCycle`) it exhibits the pullback of every principal-divisor generator as a principal-divisor generator on the total space (`VectorBundle.pullbackBundle_divisor_mem_totalRationalRelations`), hence `VectorBundle.totalRationalRelations_pullbackBundle`, the `DescendingMap` `VectorBundle.ofBundle` and the flat pullback on rational Chow groups `VectorBundle.chowPullbackBundle : A_i(Spec R) →ₗ[ℚ] A_{i+r}(E)` with `chowPullbackBundle_quotientMap`, proved invariant under isomorphism of bundles (`VectorBundle.chowEquivOfAlgEquiv_comp_chowPullbackBundle`) and therefore independent of the trivialization; the base is not assumed irreducible or reduced, only `R` Noetherian and the rank finite. Still missing on top of this are homotopy invariance (surjectivity of `chowPullbackBundle`), the positive-rank zero-section Gysin inverse, and with them Chern classes, the projective-bundle formula and the Whitney sum formula. `IntersectionTheory/EtalePullback.lean` adds the flat pullback along étale morphisms: `AlgebraicCycle.pullbackEtale` pulls back rational cycles along an arbitrary étale morphism with all multiplicities one, local finiteness of the support being proved from local quasi-finiteness (`Curves.locallyQuasiFinite_of_etale`) by restricting to an affine chart over an affine chart, where the morphism is quasi-finite and quasi-compact; additivity, ℚ-linearity, functoriality (`pullbackEtale_id`, `pullbackEtale_comp`), agreement with `pullbackOpen` on open immersions and injectivity for surjective morphisms are proved, and `cyclesOfDimension.flatPullbackEtale` is the graded version under the explicit relative-dimension-zero hypothesis; `height_le_height_base_of_etale` and `DimensionFunction.apply_le_of_etale` prove unconditionally that the closure dimension does not decrease along an étale morphism, the reverse inequality being proved for specializing étale morphisms (`DimensionFunction.apply_eq_of_etale_of_specializingMap`) and false in general since an open immersion is étale, so `EtalePresentationGroupoid` keeps the two relative-dimension-zero statements as data exactly as `OpenPresentationGroupoid` and carries the Vistoli package (`cycles`, `mem_cycles_iff`, `relations`, `chow`, `quotientMap`, `chowEquivSchemeChow`), with `OpenPresentationGroupoid.toEtale` showing that the two theories agree (`toEtale_cycles`, `toEtaleChowEquiv`); finally `DeligneMumfordStack.chosenEtaleAtlasSelfOverlap` produces the arrow scheme `R = U ×_X U` of a chosen étale surjective atlas with both projections proved étale and surjective, and `DeligneMumfordStack.etalePresentation`/`vistoliChow` give the first genuine Vistoli rational Chow group of a stack in the repository, conditional only on the two dimension gradings and their leg-compatibility, which `etalePresentationOfSpecializing` supplies from specializing legs. Still missing is the pullback of rational equivalence along étale morphisms (a comparison of orders of vanishing on a possibly non-integral étale cover), without which the Vistoli groups have no refinement functoriality and the descent sequence `0 → A(X) → A(U) ⇉ A(R)` cannot be shown exact. `IntersectionTheory/LocalizationExact.lean` completes the localization sequence in the middle: restriction of a cycle along a closed immersion is constructed (`AlgebraicCycle.pullbackClosed`) and a cycle vanishing off the image of a closed immersion is proved to be the pushforward of its restriction, in graded form `cyclesOfDimension.properPushforward_pullbackClosed`; the closure step of Fulton, Prop. 1.8, is proved rather than assumed: the scheme-theoretic image of `Z → V → X` is proved reduced, irreducible and locally Noetherian (`Scheme.isReduced_image`, `irreducibleSpace_image`, `isLocallyNoetherian_image`), hence an integral closed subscheme `IntegralClosedSubscheme.closureIn`, the comparison from `Z` to the trace of that closure on `V` is an isomorphism (`isIso_traceLift`, from the lemma that a closed immersion with dense image into a reduced scheme is an isomorphism), so `Z → Z̄` is a dominant open immersion with range exactly the part of `Z̄` over `V`, its function field is canonically identified (`Scheme.dominantFunctionFieldEquiv`), and `RationalFunctionGenerator.pullbackOpen_divisor_closureIn` proves that the principal divisor of the closed-up generator restricts to the given one; consequently `totalRationalRelations_le_map_pullbackOpen` proves, for a Noetherian `X`, that every principal-divisor relation on an open subscheme is the restriction of a relation on `X`, and `RationalEquivalenceSystem.DescendingMap.ker_openImmersionPullback_le_range` with its specialisations proves `ker(A_i(X) → A_i(U)) ≤ im(A_i(Y) → A_i(X))`, `ker_openImmersionPullback_eq_range_of_homogeneous` giving the exactness equality together with the composite-zero statement, plus the corollary `openImmersionPullback_bijective_of_forall_ne` that restriction is an isomorphism when the complement carries no point in dimension `i`. Exactly one input is still assumed, as an explicit theorem hypothesis: that the ungraded canonical span of principal divisors is graded (`hproject`, implied by `PrincipalDivisorsHomogeneous` via `project_mem_totalRationalRelations_of_homogeneous`), the catenarity/equidimensionality statement that a Weil divisor on an integral closed subscheme `W` lives in dimension `dim W − 1`, which Mathlib v4.33.1 lacks for general schemes and which `ChowGroup.lean` deliberately avoids in its definition of rational equivalence |
| 3: cotangent complexes | `CotangentComplex/*`, `CotangentComplex/PerfectRank.lean` |  Mathlib derived categories, homology-defined amplitude, affine presentation/conormal comparison, flat base change in the affine model, and a constructor sending an actual affine presentation to its derived object; `CotangentComplex/PerfectComplex.lean` now adds the affine (module) model of perfectness as fixed definitions rather than fields: `IsFiniteFree` (Mathlib freeness plus finiteness), `IsStrictlyPerfect` (a bounded complex all of whose terms are finite free), `IsPerfect E` (existence of a strictly perfect complex together with an actual isomorphism `Q(K) ≅ E` in `DerivedCategory (ModuleCat R)`), and `HasTorAmplitudeIn` (vanishing cohomology of the termwise tensor with an arbitrary module). Proved: a finite free module in one degree is strictly perfect and perfect; shifts, finite direct sums and mapping cones of strictly perfect complexes are strictly perfect (two-out-of-three for the strict notion); base change along an arbitrary ring homomorphism preserves strict perfectness (with the missing `ModuleCat.extendScalars` additivity instance supplied here); shifts and biproducts of perfect derived objects are perfect; a complex supported in `[a,b]` has Tor amplitude in `[a,b]`. The rank is the honest alternating `finsum` of `Module.finrank`, proved invariant under isomorphism, additive on direct sums and on cones (`rank(cone φ) = rank G − rank F`), multiplied by `Int.negOnePow n` under shift, and equal to `rank(K⁰) − rank(K⁻¹)` in the two-term range. `GlobalTwoTermResolution E` stores an actual `[-1,0]` complex of finite free modules plus an actual derived isomorphism onto `E`, with transport along derived isomorphisms proved functorial (`replace_refl`, `replace_trans`), rank-invariant, and implying perfectness and Tor amplitude in `[-1,0]`; an affine presentation whose conormal and ambient differential modules are finite free supplies such a resolution of its own derived object, with virtual rank `finrank(CotangentSpace) − finrank(Cotangent)`. Still absent: a geometric cotangent complex and its Jacobi--Zariski transitivity triangle, perfectness local on an atlas (no derived category of module sheaves on a stack site), the tensor product of two strictly perfect complexes in general (Mathlib's `HomologicalComplex.tensorObj` presents `⨁_{p+q=n} K^p ⊗ L^q` as a coproduct over an infinite index set and the comparison with the finite biproduct of its finitely many nonzero summands is unavailable), homotopy invariance of the termwise dual and hence representative-independence of the dual of a perfect object, and a derived base-change functor on `DerivedCategory (ModuleCat R)` itself; the arbitrary-predicate global-resolution constructor stays inactive; `CotangentComplex/PerfectDual.lean` adds the dual, tensor, triangle and base-change operations on the affine model, all as constructions: the termwise dual `dualComplex K` with `(K^∨)^i = (K^{-i})^∨` and the transposed differential carrying the standard sign `(-1)^{i+1}`, proved to preserve strict perfectness, to be contravariantly functorial (`dualComplexMap`), to satisfy `rank (K^∨) = rank K` for the honest alternating `finsum`, and to admit a biduality isomorphism `K ≅ K^∨∨` given by evaluation twisted by `(-1)^i` (the twist is what makes the squares commute, and termwise it is Mathlib's reflexivity of finite free modules); the termwise tensor product with a finite free module, proved strictly perfect with `rank (K ⊗ M) = rank K · finrank M`, together with the tensor by a finite free module in a single degree `n` and its sign `(-1)^n`; two-out-of-three for perfectness in a distinguished triangle of `DerivedCategory (ModuleCat R)` in all three positions (`IsPerfect.of_distTriang₁/₂/₃`), obtained by realising the derived morphism between two strictly perfect complexes as an actual chain map through K-projectivity (`exists_chainMap_of_derivedMap`), taking its mapping cone, and comparing triangles with Mathlib's triangulated five lemma, with the rank identity `rank E' = rank E + rank E''` proved for the resulting resolutions; stability of perfect objects under cones; and derived base change of a global two-term resolution, which is again an actual `[-1,0]` complex of finite free `S`-modules resolving its own derived image, is rank-preserving (`rank_baseChange`, `baseChange_virtualRank`), and is independent of the chosen resolution (`nonempty_baseChange_iso`, proved through `quasiIso_baseChangeFunctor_map`); the shifted dual `K^∨[1]` and the tensor product of a two-term resolution with a finite free module are again two-term resolutions, with virtual ranks `-virtualRank` and `virtualRank · finrank M`; `CotangentComplex/SquareZero.lean` adds affine square-zero deformation theory as constructions, with no result-bearing field: for a square-zero ideal `M` of an `R`-algebra `B` (the class `IsSquareZero`), the kernel is given genuine module structures over every algebra over `B ⧸ M` (`Coeff`, built from `Module.IsTorsionBySet.module`), the lifting problem `Lift R M A` for an `R`-algebra map `A → B ⧸ M` is defined, and its solutions are proved to form an actual `AddTorsor` under `Derivation R A (Coeff M)` once one solution exists (`Lift.sub`, `Lift.vadd`, `Lift.instAddTorsor`, `Lift.equivDerivation`); existence for formally smooth `A` and uniqueness for formally unramified `A` are derived from Mathlib (`nonempty_lift`, `subsingleton_lift`, `unique_lift`), and the automorphisms of the extension, the endomorphisms of `B` that are the identity on `B ⧸ M` and on `M`, are proved to be exactly `Der_R(B ⧸ M, M)` compatibly with composition and to be bijective (`Aut.equivDerivation`, `Aut.derivation_comp`, `Aut.bijective`); for an actual presentation `P : Algebra.Extension R A` the obstruction cocycle `obstructionMap` of a lift on the ambient ring is constructed as an honest `A`-linear map `I ⧸ I² → M`, the coboundaries `coboundary` are proved to be exactly the maps factoring through Mathlib's cotangent complex `I ⧸ I² → A ⊗ Ω[P.Ring⁄R]`, i.e. through the differential of this layer's `AffinePresentation.twoTerm` (`mem_coboundary_iff_comp_cotangentComplex`, `mem_coboundary_iff_comp_twoTerm`), so `ObstructionGroup` is the actual cokernel of `Hom_A(A ⊗ Ω[P.Ring⁄R], M) → Hom_A(I ⧸ I², M)`; the class `obstruction` is proved independent of the chosen ambient lift (`obstruction_eq`) and `obstruction_eq_zero_iff` proves that it vanishes if and only if the lifting problem for `A` is solvable, unconditionally for a polynomial presentation (`nonempty_lift_iff_obstruction_generators`), and `obstructionGroupMap_obstruction` proves naturality of the class under a pushforward of the coefficient module along a morphism of square-zero extensions; `CotangentComplex/SquareZeroExt.lean` now supplies both, as constructions with no result-bearing field: `restrictHom`/`coboundary_eq_range`/`obstructionGroupEquivExtOne` present `ObstructionGroup R M P` as the literal cokernel of `Hom_A(K⁰,M) → Hom_A(K⁻¹,M)` for the two-term complex `K = [I ⧸ I² → A ⊗ Ω]` of `AffinePresentation.twoTerm`; `chainHomEquiv` identifies the chain maps `K ⟶ M[1]` with `Hom_A(I ⧸ I², M)`, `homotopyOfFactor` and `eq_of_homotopy_to_coeffComplex` construct a homotopy from a factorisation through the differential and conversely, so `nonempty_homotopy_iff` proves that two such chain maps are homotopic exactly when their difference is a coboundary and `homotopyExtOneEquiv` identifies `ObstructionGroup R M P` with `Hom_{K(ModuleCat A)}(K, M[1])` with no hypothesis on the presentation; when both terms of `K` are projective, `isKProjective_presentationComplex` (from `CochainComplex.isKProjective_of_projective`) upgrades this through `CochainComplex.IsKProjective.Qh_map_bijective` to the actual derived group, `derivedExtOneEquiv : ObstructionGroup ≃ (Q K ⟶ Q M[1])` and `obstructionGroupEquivDerivedExt : ObstructionGroup ≃ DerivedExt (AffinePresentation.derivedObject R A P) M 1`, with `obstructionGroupEquivDerivedExt_obstruction_eq_zero_iff` proving that the class in that derived `Ext¹` vanishes if and only if the lifting problem for `A` is solvable; dually `cotangentH0Equiv` identifies `H⁰K` with `Ω[A⁄R]`, `extZeroEquivDerivation` identifies `Ext⁰ = Hom_A(H⁰K, M)` with `Der_R(A,M)`, `exists_unique_extZeroVAdd` proves that the lifts form a simply transitive `Ext⁰`-set, and `chainHomEquivZero`/`extZeroEquivSubtype`/`homotopyExtZeroEquiv`/`derivedExtZeroEquiv` realise this `Ext⁰` as `Hom_{K(ModuleCat A)}(K, M[0])` and, under the same projectivity, as `Hom_{D(ModuleCat A)}(Q K, Q M[0])`. Base change of the extension along `R → R'` is constructed: `baseIdeal` is the ideal of `R' ⊗[R] B` generated by `M`, proved square zero with no flatness hypothesis (`isSquareZero_baseIdeal`), `range_baseCoeffMap` identifies it with the image of `R' ⊗[R] M` and `baseIdealEquiv` upgrades this to an isomorphism `R' ⊗[R] M ≅ M'` when `R'` is `R`-flat, `basePush` exhibits `B → R' ⊗[R] B` as a morphism of square-zero extensions over `A`, and `obstruction_baseChange` proves that the obstruction class of the base-changed problem is the image of the original under `obstructionGroupMap`. The lifting groupoid is constructed rather than assumed: `Aut.instGroup` makes the automorphisms of the extension a group, isomorphic to the additive group `Der_R(B ⧸ M, M)` via `Aut.mulEquivDerivation`; `LiftHom` and `instGroupoidLift` make the solutions of the lifting problem an actual `CategoryTheory.Groupoid`; `nonempty_liftHom_iff` proves that two lifts are isomorphic exactly when their difference derivation lies in the image of `autDerivationMap : Der_R(B ⧸ M, M) →+ Der_R(A, M)`; `liftIsoClassEquiv` identifies the set of isomorphism classes with `Der_R(A, M)` modulo that image once one lift exists; and `endEquivKer` identifies the automorphism group of every lift with `ker autDerivationMap`, compatibly with composition (`endEquivKer_comp`). The point case is `obstruction_eq_zero_of_formallySmooth`, `obstruction_mvPolynomial_eq_zero` and `nonempty_lift_mvPolynomial` (every square-zero lifting problem for a polynomial algebra is unobstructed), together with `subsingleton_obstructionGroup_of_ker_eq_bot` for the tautological presentation. Still absent here: the derived `Ext¹` identification for a presentation whose conormal module is not projective (this needs the stupid-truncation distinguished triangle of a two-term complex *together with a computation of its connecting morphism*; Mathlib v4.33.1's `Algebra/Homology/Embedding/StupidTrunc.lean` defines only the truncation functor and lists the inclusion and projection as TODO, and `DerivedCategory.triangleOfSES` comes with no formula for its third map), a comparison with `Full.cotangentExt` of the presentation-independent `L_{A/R}`, and base change of the source algebra `A` itself (`SquareZero.Push` fixes the source algebra, so only the coefficients are base changed); `Modules/Derived.lean` now sets up the derived-category operations on the module categories the repository actually has: for any abelian `A` with a chosen derived category it exposes Mathlib's cohomology functors `cohomologyFunctor A n` together with the factorisation through `Q`, the cohomology-of-a-shift isomorphism `cohomologyShiftIso` with both shift-sequence coherences (`cohomologyShiftIso_zero`, `cohomologyShiftIso_add`), the canonical truncation functors `τ≤n`, `τ≥n`, and the truncation triangle (the cokernel sequence `τ≤n K ↪ K ↠ K/τ≤n K` is proved short exact, its image `truncLETriangle` is proved distinguished, the third term is identified with `τ≥(n+1) K` in the derived category (`truncLEX₃IsoTruncGE`), and the resulting `truncTriangle K n₀ n₁ : τ≤n₀K ⟶ K ⟶ τ≥n₁K ⟶ (τ≤n₀K)⟦1⟧` is proved distinguished with its first two maps computed as `Q(ιTruncLE)` and `Q(πTruncGE)`), with `Ext` available via `hasExt`; these are instantiated both for `ModuleCat R` and for sheaves of modules on a ringed site (`modulesAbelian` records that Mathlib's abelian structure on `SheafOfModules` applies under the two sheafification hypotheses already carried by `Modules/Stack.lean`, so no abelianness is assumed); derived base change along a ring map `f : R →+* S` is constructed as the termwise `extendScalars` functor on cochain complexes and proved well defined on quasi-isomorphisms (bounded-above complexes of projectives are K-projective, `isKProjective_of_isStrictlyLE` and `isKProjective_of_isStrictlyPerfect`, base change preserves homotopies and homotopy equivalences, hence a quasi-isomorphism between such complexes stays a quasi-isomorphism after base change, `quasiIso_baseChangeFunctor_map`, and induces `baseChangeDerivedIso`); pseudofunctoriality is proved rather than assumed: the identity and composition comparisons `baseChangeIdIso`, `baseChangeComp'` are the mates of Mathlib's `restrictScalarsId`/`restrictScalarsComp'` under the extension/restriction adjunction, their components are computed on pure tensors, and the left unit, right unit and associativity coherence laws are proved (`baseChangeComp'_id_left`, `baseChangeComp'_id_right`, `baseChangeComp'_assoc`); still absent are the comparison of base change with the derived tensor by a complex of free modules (Mathlib has no natural isomorphism `extendScalars f ⋙ restrictScalars f ≅ − ⊗_R f_*S` compatible with the `Module.compHom` instance used by the `⊗ₜ[R,f]` notation) and the derived tensor/internal Hom/duality `RHom(E,R)` of strictly perfect complexes with `rank_dual` (Mathlib has no linear-dual functor `(ModuleCat R)ᵒᵖ ⥤ ModuleCat R`, and the monoidal structure on `CochainComplex (ModuleCat R) ℤ` is not available as instances here); `CotangentComplex/Full.lean` now constructs the cotangent complex of a ring map itself: for `R → S` in one universe, `Full.cotangentComplex R S` is the actual object of `DerivedCategory (ModuleCat S)` obtained by localising the two-term presentation complex of `Algebra.Generators.self R S`, and its independence of the presentation is proved rather than assumed: a morphism of `Algebra.Extension`s induces an honest chain map `Full.map` which is strictly functorial (`map_id`, `map_comp`), any two morphisms induce chain homotopic maps via an actual `Homotopy` built from Mathlib's `Algebra.Extension.Hom.sub` (`Full.homotopy`), consequently `Q_map_eq_of_homotopy` and `derivedMap_eq` show the induced derived morphism `derivedMap` is canonical (it does not depend on the chosen morphism of extensions), and `derivedIso` upgrades a pair of opposite morphisms to an isomorphism with full coherence (`derivedIso_refl`, `derivedIso_trans`, `derivedIso_symm`, `derivedIso_eq`); for arbitrary polynomial presentations this yields `generatorsIso` with `generatorsIso_refl`/`generatorsIso_trans`/`generatorsIso_symm` and `presentationIso : L_{S/R} ≅ AffinePresentation.derivedObject R S P.toExtension` with `presentationIso_trans`/`presentationIso_self`, so the affine comparison with `AffinePresentation.twoTerm` is a derived isomorphism and the transitivity maps are natural, and `comparePresentation` gives a canonical map to the derived object of every extension via `Algebra.Extension.defaultHom`; the cohomology is computed as actual isomorphisms (`homologyNegOneIso`/`cohomologyNegOne` identify `H⁻¹` with Mathlib's `Algebra.H1Cotangent R S`, `homologyZeroIso`/`cohomologyZero` identify `H⁰` with `Ω[S⁄R]` via the cokernel of the conormal differential and `toKaehler`, and `isZero_cohomology`/`hasCohomologicalAmplitude` give amplitude `[-1,0]` in this layer's sense), from which the criteria are proved: `formallyUnramified_iff` (`H⁰ = 0`), `formallySmooth_iff` (`H⁰` projective and `H⁻¹ = 0`), `formallyEtale_iff`, and `formallyEtale_iff_isZero` (`L_{S/R} ≅ 0`, proved through `isZero_Q_obj`: an acyclic complex has zero image under `Q`), whence `isZero_cotangentComplex_of_isLocalization` shows the cotangent complex of a localisation vanishes; the Jacobi--Zariski sequence for `R → S → T` is available as actual maps `jzH1Map`, `jzDelta`, `jzBaseChange`, `jzOmegaMap` between the cohomology objects of the three cotangent complexes together with exactness at every spot (`exact_jzH1Map_jzDelta`, `exact_jzDelta_jzBaseChange`, `exact_jzBaseChange_jzOmegaMap`, `surjective_jzOmegaMap`); base change is proved in the form `baseChangePresentationIso` (the base change of a polynomial presentation computes `L_{(T⊗S)/T}`), with `cohomologyNegOneBaseChangeIso`, the flat statement `h1CotangentBaseChangeEquivOfFlat` and `kaehlerBaseChangeEquiv`, and `cotangentExt` exposes `Extⁿ(L_{S/R}, M)` with `subsingleton_cotangentExt_of_formallyEtale` its vanishing in the étale case; still absent are the full untruncated simplicial cotangent complex (Mathlib v4.33.1 has no simplicial commutative algebras), Jacobi--Zariski as a distinguished triangle (Mathlib's `δ` exists only on `H¹`, with no chain-level connecting morphism), the identification `Ext¹(L_{A/R}, M) ≅ SquareZero.ObstructionGroup` (no usable `Ext`-of-a-two-term-complex or t-structure adjunction API), base change stated over `T ⊗ S` rather than `T` (no `T ⊗[R] S`-module instance on `T ⊗[R] M`), and the lci criterion (no Mathlib notion of local complete intersection linked to the conormal module); `CotangentComplex/Global.lean` now globalises the ring-map cotangent complex to affine charts of a morphism of schemes and to the charts of an étale atlas, with every comparison constructed and every coherence proved rather than assumed. The technical device is restriction of scalars to a common ground ring: Mathlib's `Algebra.Extension.Hom` is general enough to relate presentations over *different* base rings, and `restrictHom`/`mapOver` turn such a morphism into an honest chain map of presentation complexes viewed over a ring `S₀` mapping into both targets, so that `mapOver_id` and `mapOver_comp` are *strict* equalities of chain maps and the cocycle condition for refinements needs no coherence isomorphism; `homotopyOver` (built from `Algebra.Extension.Hom.sub`, which Mathlib states in the same generality) proves that two morphisms of extensions over the same base map induce chain-homotopic comparisons, whence `derivedMapOver_eq`: the induced morphism in `DerivedCategory (ModuleCat S₀)` is canonical, and `derivedMapOver_id`/`derivedMapOver_comp` make it functorial. Specialised to a tower `R → S → T` this gives `towerComparison : L_{S/R} ⟶ L_{T/R}` in `DerivedCategory (ModuleCat R)` with `towerComparison_self` and `towerComparison_trans` proved from canonicity alone. Geometrically, `AffineChart f V` stores only an affine open `U ⊆ f⁻¹V` of the source together with its affineness and containment (no embedding, no presentation, no cotangent complex is a field) and `AffineChart.cotangentComplex` is the actual object `Full.cotangentComplex Γ(Y,V) Γ(X,U)` of `DerivedCategory (ModuleCat Γ(X,U))`, with `AffineChart.objectOverBase` its avatar over `Γ(Y,V)`; `AffineChart.comparison` is the restriction morphism of a refinement `U' ⊆ U` (the scalar tower `isScalarTower_res` is proved from `Scheme.Hom.appLE_map`, not assumed) and `AffineChart.comparison_trans` proves that comparison maps commute with chart refinements. The local model is exhibited: `localComplex_X_negOne`, `localComplex_X_zero`, `localComplex_d`, `localComplex_isZero` identify the presentation complex with `[I/I² → Ω_{M/V}|_U] = [P.ker.Cotangent → Γ(U) ⊗_{Γ(M_U)} Ω_{Γ(M_U)/Γ(V)}]` in degrees `-1,0`, `embeddingIso`/`embeddingIso_hom`/`isIso_comparePresentation` make the comparison with `L_{Γ(U)/Γ(V)}` an actual isomorphism in the derived category for an embedding into an affine space over `V`, with `embeddingIso_trans`/`embeddingIso_self` proving that two embeddings give coherent comparisons and `embeddingCotangentSpaceBasis` exhibiting degree `0` as free on the coordinate differentials; for an arbitrary formally smooth ambient ring only the cohomology comparisons `cohomologyZeroLocalIso` and `cohomologyNegOneSmoothLocalIso` (the latter via `Algebra.Extension.equivH1CotangentOfFormallySmooth`) are available, because turning `Full.comparePresentation` into a quasi-isomorphism would need a naturality statement for `ShortComplex.moduleCatHomologyIso`, which Mathlib lacks, and a morphism of extensions back to the canonical polynomial presentation, which formal smoothness does not supply (it lifts only along square-zero surjections). On the étale side, `isZero_cotangentComplex_of_etale` proves `L_{U'/U} = 0`, `cohomologyZeroEtaleIso` together with `jzBaseChange_eq_cohomologyZeroEtaleIso` and `isIso_jzBaseChange_of_formallyEtale` prove that `H⁰` is the honest base change along a formally étale map and that this isomorphism *is* the Jacobi–Zariski map, `cohomologyNegOneLocalizationIso` does the same for `H⁻¹` along a localisation, and `jzH1Map_eq_zero`/`jzDelta_eq_zero`/`jzOmegaMap_eq_zero` record the resulting collapse of the Jacobi–Zariski sequence; `AffineChart.basicOpenChart` with `isLocalization_basicOpenChart`, `formallyEtale_basicOpenChart`, `isZero_cotangentComplex_basicOpenChart`, `cohomologyZeroBasicOpenIso`, `cohomologyNegOneBasicOpenIso` instantiate all of this for Zariski refinements, and `AffineChart.coverChart`, `etale_appLE` (from `HasRingHomProperty.appLE` for `Etale`), `isZero_cotangentComplex_etaleCover` and `cohomologyZeroEtaleCoverIso` for an étale cover of a chart. For Deligne–Mumford stacks `etale_atlasPullbackPresentation_fst` extracts from `StackChart.IsEtaleSurjective` that every base change of the atlas is an actual scheme with étale projection, and `isZero_cotangentComplex_atlasTransition`, `cohomologyZeroAtlasTransitionIso` and `exists_etaleAtlas_isZero_cotangentComplex_transition` package the affine-local complexes on the atlas together with the proved vanishing of every chart-transition complex and the degree-zero base change; the last statement produces the atlas from the stack's own `etaleAtlas` field, so no atlas and no embedding occurs in its formulation. Still absent: the comparison maps are not proved to be isomorphisms after derived base change (this needs the comparison of `Modules.Derived.baseChangeFunctor` with the derived tensor product, already recorded as unavailable), `H⁻¹` base change along a general étale rather than localising map (this needs a compatible pair of presentations satisfying the bijectivity hypothesis `J ≃ Q ⊗_P I` of `Algebra.Extension.tensorH1CotangentOfFormallyEtale`), the smooth-embedding comparison for a non-polynomial smooth ambient, refinement of the target open `V` (which is the Jacobi–Zariski direction and would need the triangle, not just the exact sequence), comparison across two *different* atlases (`Stacks/AtlasRefinement.lean` supplies only 2-categorical common refinements, with no passage from a refinement 2-cell to a scheme morphism of chart schemes), and any assembly of the chart family into a sheaf of complexes on `Sites.smallEtaleStackRingedSite`, which would require the derived category of `SheafOfModules` together with effectivity of descent data and a resolution of the universe mismatch between that site's `CommRingCat.{u+1}`-valued structure sheaf and the single-universe `Full.cotangentComplex`; `CotangentComplex/PerfectDualInvariance.lean` closes the homotopy-invariance gap of the termwise dual: `dualHomotopy` transposes a homotopy of chain maps through the sign convention of `dualComplex`, so `dualComplexMap` preserves homotopy equivalences (`dualHomotopyEquiv`) and descends to a contravariant functor `dualFunctor` on the homotopy category of cochain complexes of `R`-modules; combined with K-projectivity of strictly perfect complexes, `exists_homotopyEquiv_of_derivedIso` shows that every isomorphism `Q K ≅ Q L` of strictly perfect complexes is `Q` of a homotopy equivalence and `quasiIso_dualComplexMap` that duals of quasi-isomorphisms are quasi-isomorphisms, hence the dual of a perfect object is well defined: `IsPerfect.dualObject` dualises a chosen strictly perfect representative, `IsPerfect.dualObjectIso` identifies the dual of any other representative with it, the comparison isomorphisms `dualIso` are independent of the chain map realising them (`dualIso_hom_eq`) and compose contravariantly (`dualIso_refl`, `dualIso_trans`), the dual object is perfect (`isPerfect_dualObject`), biduality holds (`bidualObjectIso`), and duality is fully faithful on strictly perfect complexes (`dualDerivedHomEquiv`, `dualPairingSymmEquiv`, via `bidualityIso_naturality`); still missing are the invariance of the alternating rank under homotopy equivalence (the Euler-characteristic formula), isolated as the explicit hypothesis `RankHomotopyInvariant` of `IsPerfect.rank_eq_of_rep` and `IsPerfect.rank_dualObject` (unconditionally only `rank (dualComplex h.rep) = h.rank` is proved), and the identification of `Hⁱ(K^∨)` with the Ext groups `Hom_D(Q K, Q(R)⟦i⟧)` (Round 12.) `CotangentComplex/PerfectRank.lean` discharges the hypothesis `RankHomotopyInvariant` of `PerfectDualInvariance.lean` for every commutative ring: `rank_eq_of_homotopyEquiv` proves that homotopy equivalent strictly perfect complexes have the same rank. The proof shows first that a contractible strictly perfect complex has rank zero (`rank_eq_zero_of_homotopy_id_zero`) by base changing to the residue field of a maximal ideal, where the contracting homotopy makes the complex exact and the alternating sum of dimensions of a bounded exact family of finite-dimensional vector spaces vanishes by rank–nullity and telescoping (`finsum_negOnePow_finrank_eq_zero`, `finrank_eq_boundaryRank_add`); then a homotopy equivalence has contractible mapping cone, because in the homotopy category the cone of an isomorphism is a zero object (`Pretriangulated.Triangle.isZero₃_iff_isIso₁` on the distinguished cone triangle, `HomotopyCategory.isZero_quotient_obj_iff`), and rank is additive on cones. Hence `rankHomotopyInvariant : RankHomotopyInvariant R`, and the rank of a perfect object of `D(R)` is unconditionally well defined on every strictly perfect representative (`IsPerfect.rank_eq_of_rep'`) and equal to the rank of its dual (`IsPerfect.rank_dualObject'`). |
| 4: cone stacks | `Cones/TwoTermQuotient.lean`, `Cones/Picard.lean`, `Cones/DerivedPicard.lean`, `Cones/Stack.lean`, `Cones/Quotient.lean`, `Cones/Criteria.lean`, `Cones/CriteriaBundle.lean`, `Cones/DerivedCriteria.lean`, `Cones/DerivedTruncation.lean`, `Cones/DerivedTruncationRoof.lean`, `Cones/PerfectTwoTerm.lean`, `Cones/DualShortExact.lean`, `Cones/QuotientTorsor.lean`, `Cones/StackFibreProducts.lean`, `Cones/StackCoherence.lean` |  actual Picard groupoids, fixed-base groupoid fibres, pullback-natural addition/actions, contraction and stabilizers, chain-homotopy 2-isomorphisms, quasi-isomorphism equivalences, an explicit `[K ≃ K]` acyclic-summand homotopy equivalence, vector-bundle-stack ranks derived from an actual local finite-free matrix quotient presentation, and a constructed rank-zero bundle stack whose fibres are explicitly equivalent to `[0/0]`. `Cones/DerivedPicard.lean` now makes `h¹/h⁰` functorial and derived: two-term complexes of `R`-modules are given a `Category` instance and `h¹/h⁰` is an honest functor `picardFunctor : LinearTwoTermComplex R ⥤ Cat`, with specified pseudofunctor comparison 2-cells (`picardCompIso`, `picardIdIso`) whose associativity and both unit coherence laws are proved; they are identities, and strictness is proved from the chain-level formulas rather than assumed. Chain homotopies are the invertible 2-cells: identity, vertical composition, inverse, both whiskerings and horizontal composition are constructed (`ChainHomotopy.refl/vcomp/symm/postcomp/precomp/hcomp`) and `ChainHomotopy.natTrans` is proved to take each of them to the corresponding operation on natural transformations (`natTrans_refl`, `natTrans_vcomp`, `natTrans_symm`, `natTrans_postcomp`, `natTrans_precomp`, `natTrans_hcomp`). A cochain complex of modules has a functorial underlying two-term complex in degrees `-1,0` (`cochainFunctor`, `cochainPicardFunctor`), and Mathlib homotopies and homotopy equivalences of such complexes restrict to the repo's chain-level notions (`ofHomotopy`, `ofHomotopyEquiv`). For a derived object `E` with a `GlobalTwoTermResolution F`, `h¹/h⁰(E)` is defined as `F.picard`, with vertex automorphisms proved to be `h⁰` (the kernel of the differential) and isomorphism classes proved to be `h¹` (its cokernel). Resolution independence is proved unconditionally: the resolving complex is proved K-projective (bounded above, free hence projective terms), so Mathlib's `CochainComplex.IsKProjective.Qh_map_bijective` lifts the derived comparison isomorphism of any two global two-term resolutions of the same `E` to an actual chain map realizing it (`exists_chain_realization`), that chain map is a quasi-isomorphism and therefore a homotopy equivalence of cochain complexes (`exists_homotopyEquiv`), and hence the two Picard groupoids are equivalent (`hasChainComparison`, `nonempty_picardEquivalence`), with the explicit hypothesis form `Comparison`/`HasChainComparison` retained for the future sheaf-level statement and proved stable under identity, composition and transport along `E ≅ E'`. Functoriality in composites is proved on the nose in the forward direction and up to a canonical natural isomorphism on inverses (`quotientEquivalence_comp_functor`, `quotientEquivalenceCompInverseIso`, `transPicardEquivalenceInverseIso`, all from uniqueness of right adjoints), and the inverse of the equivalence attached to a quasi-isomorphism is proved to agree with the chain-level inverse (`HomotopyEquivalence.quasiIsoInverseIso`), in particular with the projection off the acyclic summand `[K ≃ K]`, which is also proved to be a strict retraction (`AcyclicSummand.inverseIsoProjection`, `inclusion_quotientFunctor_comp_projection`). Still absent: canonicity of the comparison equivalence (the realizing chain map is unique only up to homotopy, so the equivalence is canonical only up to the `ChainHomotopy.natIso` 2-cells, and no uniqueness statement is proved); a comparison of the elementwise kernel/cokernel quasi-isomorphism predicate with Mathlib's `QuasiIso`, which would need the homology of a `[-1,0]`-complex of `ModuleCat R` identified elementwise with the kernel and cokernel of its differential; and any sheaf-level version, since there is no derived category of module sheaves on a stack site; `Cones/Quotient.lean` additionally constructs the quotient `[C/E]` of an affine cone by a vector-bundle action as an actual groupoid, fibrewise over affine test schemes and with no conclusion stored as presentation data: `ConeQuotient.QuotientGroupoid A B` has as objects the `B`-points of `C` and as arrows the `B`-points of `E` translating one point into the other, with constructed `Category` and `Groupoid` instances, and the hom-sets are proved to be exactly the orbit translations and the automorphism groups exactly the stabilisers (`homEquiv`, `nonempty_hom_iff`, `autEquivStabilizer`), so nothing is collapsed or enlarged; the projection `C → [C/E]` is `QuotientGroupoid.of`, the vertex is `vertexPoint` (the image of the cone vertex), and the contraction action on the quotient is constructed as `contractionFunctor`, well defined precisely because of the proved `𝔾ₘ`-equivariance of the action, with its laws constructed as natural isomorphisms `contractionOneIso`, `contractionMulIso`, `contractionZeroIso` (contraction by `0` is the constant functor at the vertex) and `contractionFunctor_vertexPoint`; reindexing along a map of test algebras is `mapQuotient` and is proved strictly functorial (`mapQuotient_id`, `mapQuotient_comp`) and to commute with contraction (`mapQuotient_comp_contractionFunctor`), which is packaged as an honest functor `quotientPrestack : CommAlgCat R ⥤ Cat`, the affine quotient prestack; an equivariant morphism induces `quotientFunctor : [C'/E'] ⥤ [C/E]`, strictly compatible with identities, composition and reindexing, and a homotopy between two equivariant morphisms (a map `H : C' → E` carrying `φ₁` to `φ₂` whose failure of `E'`-invariance is exactly the difference `u₂ - u₁` of the bundle maps, `ConeQuotient.Homotopy`) is proved to induce the expected invertible `2`-cell `Homotopy.natIso`; the criterion for two quotient presentations to have equivalent quotients is proved as a theorem rather than stored, `isEquivalence_quotientFunctor` deducing `Functor.IsEquivalence` from orbit-surjectivity, injectivity of `l' ↦ l' ∘ u` on translations and liftability of translations; that the construction really is the torsor quotient is verified in two cases, `translationQuotientEquivalence` proving `[E/E] ≃ Spec R` (the translation action is simply transitive, `translationAction_hom_unique`) and `freeQuotientEquivalence` proving `[C ×_{Spec R} E / E] ≃ C`, and the general statement is packaged as `IsExactSequence` for a sequence `E → C → D` (invariance, surjectivity on points, fibres equal to orbits, freeness, all hypotheses and none a stored conclusion) with `IsExactSequence.quotientEquivalence` proving that the quotient groupoid is then the discrete groupoid of `D`-points and `isExactSequence_freeAction` exhibiting `E → C × E → C` as exact; what remains is `[C/E]` as an `FppfStack`, hence as a `ConeStack` with `StackHom` projection and vertex and with `contractionPullbackIso`, because `Stacks/QuotientStack.lean` still exports only the torsor groupoids `ActionTorsor` and their `pullbackPseudofunctor` and the fppf stackification of a groupoid-valued prestack is absent from the repository and from Mathlib, the passage from the algebra-points prestack to a stack on all test schemes (which needs `fppfYoneda.obj (Spec S)` identified with the functor of `R`-algebra points), the geometric form of the equivalence criterion (cartesian square with `E' → E` surjective and `C' = C ×_E E'`, since a surjection of vector bundles is not surjective on `B`-points for an arbitrary ring `B` and the hypotheses of `isEquivalence_quotientFunctor` can only be discharged fppf-locally), fibre products of cone actions (blocked by the missing `Sym(F ⊕ F') ≃ Sym(F) ⊗ Sym(F')`), and the comparison of `[C/E]` with the `h¹/h⁰` Picard groupoid of `Cones/DerivedPicard.lean`, which needs the sheaf-level quotient; `Cones/Criteria.lean` now proves the cohomological criteria that convert statements about `h⁰` and `h¹` into statements about the `h¹/h⁰` groupoid, with no conclusion stored in a structure field: the automorphism group of every object is `h⁰` (`PicardCriteria.autEquivKernel`), every nonempty hom-set is an `h⁰`-torsor (`homEquivKernel`), isomorphism classes are exactly `h¹` (`isoClass`, `isoClass_eq_iff`, `isoClass_surjective`, `isoClass_map`), and for a chain map `φ` the induced functor is faithful iff `h⁰(φ)` is injective (`faithful_iff`), full iff `h⁰(φ)` is surjective *and* `h¹(φ)` is injective (`full_iff`), essentially surjective iff `h¹(φ)` is surjective (`essSurj_iff`), and an equivalence iff `φ` is a quasi-isomorphism (`isEquivalence_iff`), the last being the converse of the previously one-directional `quotientEquivalence`; the closed-immersion criterion is `PicardCriteria.IsCohomologicalMono` (`h⁰(φ)` bijective and `h¹(φ)` injective), proved equivalent to full faithfulness (`iff_full_and_faithful`, `fullyFaithful`), to imply injectivity on isomorphism classes (`injective_isoClass`), and, when `φ` is degreewise injective, left-cancellability of the induced functor, i.e. monomorphy (`quotientFunctor_cancel`); degreewise short exact sequences `0 → K' → K → K'' → 0` are `PicardCriteria.ShortExact`, six hypotheses and no conclusions: the connecting map `δ : h⁰(K'') → h¹(K')` is constructed from lifts and proved lift-independent (`exists_delta_lift`, `delta_well_defined`, `deltaFun_eq`, `delta`), and the six-term cohomology sequence is proved exact at every spot (`injective_kernelMap`, `exact_h0_middle`, `exact_h0_right`, `exact_h1_left`, `exact_h1_middle`, `surjective_cokernelMap`); geometrically this gives that `[K¹/K⁰] → [K''¹/K''⁰]` is essentially surjective (`essSurj_quotientFunctor`), that the fibres of the induced map on hom-sets are torsors under `h⁰(K')` with a unique translating automorphism (`hom_fibre_torsor`), that the translation action of `[K'¹/K'⁰]` (`translate`, `translate_zero`, `translate_add`, `translateInvarianceIso`) has as orbits exactly the fibres on isomorphism classes (`nonempty_iso_map_iff`), and that an acyclic quotient makes the inclusion an equivalence (`isQuasiIsomorphism_of_acyclic`, `quotientEquivalenceOfAcyclic`); `Cones/CriteriaBundle.lean` adds the finiteness and base-change half: the virtual rank `rank E¹ - rank E⁰` (`PicardCriteria.virtualRank`) is proved invariant under quasi-isomorphism of two-term complexes of finite free modules (`virtualRank_eq_of_isQuasiIsomorphism`) by exhibiting the mapping-cone sequence `0 → E⁰ → F⁰ × E¹ → F¹ → 0`, proving its exactness from the two cohomology bijections (`coneAlpha_injective`, `coneBeta_surjective`, `ker_coneBeta`) and splitting it against projectivity of `F¹`; hence the derived virtual rank of a global two-term resolution is the naive Euler characteristic of its underlying two-term complex (`rank_eq_virtualRank`, `virtualRank_resolution`) and is independent of the chosen resolution (`virtualRank_resolution_eq`, via `DerivedPicard`'s unconditional `hasChainComparison`), and the displayed rank of a `VectorBundleStack` is the virtual rank of the two-term complex of its local matrix presentation (`stackRank_eq_virtualRank`, `virtualRank_freeTwoTermComplex`); the fibres of `h¹/h⁰(Eᵛ)` over the affine objects of the big fppf site are the translation groupoids of the complexes of `B`-points `[Hom_R(E¹,B) → Hom_R(E⁰,B)]` (`dualPoints`); reindexing along a map of test algebras is postcomposition and is proved strictly functorial (`dualPointsMap_id`, `dualPointsMap_comp`), so the fibres assemble into an honest prestack `dualPrestack : CommAlgCat R ⥤ Cat` with no unproved coherence datum; a chain map dualises contravariantly (`dualHom`, `dualHom_id`, `dualHom_comp`) and commutes strictly with reindexing (`dualHom_quotientFunctor_naturality`), a chain homotopy dualises over every test algebra (`dualChainHomotopy`), so a chain homotopy equivalence induces equivalences on every fibre with no flatness hypothesis (`dualHomotopyEquivalence`, `dualQuotientEquivalence`), the fibrewise equivalence criterion is `dualIsEquivalence_iff` with its "after every base change" form `dualEquivalence_of_forall_quasiIso`, and the automorphism group of every object of the fibre over `Spec B` is proved to be `Hom_R(h¹(E), B)` (`dualH0Equiv`), which is the explicit small-to-big comparison in degree zero; still absent: the fppf stackification of `dualPrestack`, so none of these criteria is yet available as a statement about `ConeStack`s or `StackHom`s, for the same reason as for `quotientPrestack`; any statement that `h¹` of the dual commutes with base change, which is false without flatness, so the hypotheses of the criteria must be imposed test algebra by test algebra; the derived-category form, turning a distinguished triangle in `D(R)` into a `ShortExact` of two-term complexes, which needs the mapping cone of complexes of modules compared with the elementwise kernel/cokernel predicate; and the comparison of `PicardCriteria.ShortExact` with the geometric `ConeQuotient.IsExactSequence`, which needs the still-missing sheaf-level identification of `[C/E]` with the Picard groupoid (Round 12.) `Cones/DerivedCriteria.lean` closes the gap between the elementwise `h¹/h⁰` theory and Mathlib's homological algebra. `LinearTwoTermComplex.toCochainComplexHom` realizes a chain map of two-term complexes as a morphism of cochain complexes, inverse on the nose to `ofCochainComplexHom`, and `homologyNegOneIso`/`homologyZeroIso` identify the homology of a complex supported in degrees `-1, 0` with the kernel and the cokernel of its differential (all other degrees vanish, `isZero_homology_toCochainComplex`). The main comparison `isQuasiIsomorphism_iff_quasiIso` proves that the repo's elementwise predicate `Hom.IsQuasiIsomorphism` is *equivalent* to Mathlib's `QuasiIso`, equivalently (`isIso_Q_map_toCochainComplexHom_iff`) to invertibility in `D(R)`. On the derived side, `ShortExact.cochainShortExact` upgrades a degreewise short exact sequence of two-term complexes to a short exact sequence of cochain complexes, whence a distinguished triangle in `D(R)` (`ShortExact.triangleOfSES_distinguished`) and the identification of the mapping cone of the inclusion with the quotient complex (`ShortExact.quasiIso_descShortComplex`); conversely `shortExact_toCoker` builds the short exact sequence with quotient `LinearTwoTermComplex.coker φ` from any degreewise injective chain map, and `ShortExact.shortExactPicard` packages the proved Picard-groupoid consequences. Finally `GlobalTwoTermResolution.nonempty_homotopy_of_Q_map_eq` shows that a derived morphism between resolving complexes has a chain realization unique up to homotopy, so `Comparison.picardEquivalence_unique_up_to_natIso` (and its inverse counterpart) make the resolution-independence equivalence of `h¹/h⁰` canonical up to natural isomorphism. Still missing: the comparison of `ShortExact.delta` with Mathlib's connecting map `ShortComplex.ShortExact.δ` of the long exact homology sequence, and any statement over a base other than a ring. `Cones/DerivedTruncation.lean` extends `h¹/h⁰` from two-term complexes to arbitrary cochain complexes of `R`-modules. `LinearTwoTermComplex.twoTermTrunc K = [K⁻¹/im d⁻² → K⁰]` is functorial (`twoTermTruncHom`, `twoTermTruncFunctor`) and comes with a projection `truncProjection K` from `K`, which is proved to be a quasi-isomorphism in degree `-1` unconditionally and in degree `0` whenever `K.d 0 1 = 0` (`quasiIsoAt_truncProjection_negOne`, `quasiIsoAt_truncProjection_zero`); consequently `homologyNegOneIsoTrunc` and `homologyZeroIsoTrunc` identify `h⁰` and `h¹` of the truncation with Mathlib's `H⁻¹(K)` and `H⁰(K)`. From this, `isQuasiIsomorphism_twoTermTruncHom` shows that a chain map which is a quasi-isomorphism in degrees `-1` and `0` truncates to an elementwise quasi-isomorphism, so `truncQuotientEquivalence` gives an equivalence of Picard groupoids, and `ofHomotopyTrunc` descends a homotopy of chain maps to a chain homotopy of truncations needing only `K.d 0 1 = 0` — the component `K⁻¹ ⟶ L⁻²` is killed by the quotient. For derived objects, `TruncPresentation E` (a K-projective complex with no differential out of degree `0` representing `E`) defines `h¹/h⁰(E)`, and `TruncPresentation.nonempty_picardEquivalence` proves independence of the presentation by realizing the comparison isomorphism as a chain map; `GlobalTwoTermResolution.nonempty_picardEquivalence_truncPresentation` shows this agrees with the two-term construction of `Cones/DerivedPicard.lean`. The hypothesis `K.d 0 1 = 0` is necessary (otherwise `h¹` of the truncation is the cokernel of `d⁻¹`, not `H⁰`); what remains missing is independence for arbitrary, non-K-projective representatives, which would require representing a derived isomorphism by a roof of quasi-isomorphisms and truncating the intermediate complex (`nonempty_truncQuotientEquivalence_of_roof` is ready for such input). `Cones/DerivedTruncationRoof.lean` removes the K-projectivity hypothesis from the resolution independence of `h¹/h⁰`. `LinearTwoTermComplex.nonempty_truncQuotientEquivalence_of_Q_iso` proves that any two cochain complexes of `R`-modules with no differential out of degree `0` whose images in the derived category are isomorphic have equivalent Picard groupoids of two-term truncations; the proof represents the isomorphism by a roof `K ← N → K'` of quasi-isomorphisms (`DerivedCategory.right_fac`, i.e. the calculus of fractions of `HomotopyCategory.quasiIso`) and composes the legs with `N.ιTruncLE 0`, which is a quasi-isomorphism in all degrees `≤ 0` and whose source has no differential out of degree `0`. This yields a presentation-free definition of `h¹/h⁰(E)` for a derived object: `TruncRep E` is any complex with `d⁰ = 0` representing `E`, `TruncRep.picard` its Picard groupoid and `TruncRep.nonempty_picardEquivalence` the independence statement, with `TruncRep.vertexAutEquiv` identifying vertex automorphisms with `H⁻¹`. Such a representative exists for every complex without cohomology in positive degrees (`exists_rep_d_zero_one_eq_zero`, `TruncRep.ofIsLE`, via `τ≤0`), and `TruncPresentation.nonempty_picardEquivalence_truncRep` together with `GlobalTwoTermResolution.nonempty_picardEquivalence_truncRep` prove that this agrees with the K-projective presentations of `Cones/DerivedTruncation.lean` and with the two-term resolutions of `Cones/DerivedPicard.lean`. The hypothesis `d⁰ = 0` on both complexes is necessary and remains; what is still missing is a representative with `d⁰ = 0` for derived objects with nonzero positive cohomology, and the functoriality of `E ↦ h¹/h⁰(E)`. `Cones/PerfectTwoTerm.lean` proves the affine form of "`h¹/h⁰(Eᵛ)` is a vector-bundle stack when `E` is perfect of amplitude `[-1,0]`". The top of a bounded complex of finitely generated projectives that is exact in positive degrees is chopped at the level of modules: `projective_ker_of_surjective` plus the downward induction `projective_finite_cocycles` show that all cocycles `Z^i = ker d^i` with `i ≥ 0` are finitely generated projective. The bottom is chopped by `perfectTwoTerm K = [K⁻¹/im d⁻² → Z⁰(K)]`, whose degree-zero term is always finitely presented (`finitePresentation_perfectTwoTerm_degreeZero`) and is projective as soon as it is flat (`projective_perfectTwoTerm_degreeZero_of_flat`), flatness being exactly what a Tor-amplitude hypothesis provides; when the complex vanishes in degree `-2` no hypothesis is needed. The resulting property `IsVectorBundleComplex` — both terms finitely generated projective — is inherited by the complexes of `B`-points of the dual (`isVectorBundleComplex_dualPoints`, from `projective_finite_hom_of_projective_finite`), which is the fibrewise vector-bundle-stack statement, and it holds unconditionally for the global two-term resolutions of `Cones/DerivedPicard.lean` (`GlobalTwoTermResolution.isVectorBundleComplex`). On the derived side, `quasiIso_truncProjection` shows that a complex exact outside `[-1,0]` with no differential out of degree `0` is quasi-isomorphic to its two-term truncation, so `truncRepOfExact` produces a `TruncRep` of `Q.obj K` whose Picard groupoid is `h¹/h⁰` of the derived object, and `perfectQuotientEquivalence` identifies the perfect two-term model with that truncation. What remains missing is the deduction of flatness of `K⁻¹/im d⁻²` from `HasTorAmplitudeIn K (-1) 0` (it needs Tor computed by the finite projective resolution `K^a → ⋯ → K⁻¹`), and the complex-level homotopy equivalence `K ≃ L`, which none of the `h¹/h⁰` statements require. `Cones/DualShortExact.lean` turns a degreewise split short exact sequence of two-term complexes into a short exact sequence of abelian cone stacks. `PicardCriteria.ShortExact.IsDegreewiseSplit` asks for degreewise retractions of the injection; the splitting lemma (`exists_section_of_retraction`, `exists_retraction_of_section`) shows this is the same as degreewise sections of the surjection, and `ShortExact.isDegreewiseSplit_of_projective` proves that a projective third term — the two-term shadow of a perfect third term in a distinguished triangle — always splits the sequence. Dualising then gives `ShortExact.shortExact_dualComplexHom` for module coefficients and `ShortExact.shortExact_dualHom` for the complexes of `B`-points, with all six conditions proved; `shortExactPicard_dualComplexHom` and `shortExactPicard_dualHom` package the geometric consequences (essential surjectivity of `h¹/h⁰(Kᵛ) → h¹/h⁰(K'ᵛ)`, hom-fibres torsors under `h⁰(K''ᵛ)`, iso-fibres the orbits of `h¹/h⁰(K''ᵛ)`). The six-term sequence of the duals is stated through `dualModuleH0Equiv` as `0 → Hom(h¹K'', N) → Hom(h¹K, N) → Hom(h¹K', N) → h¹(K''ᵛ) → h¹(Kᵛ) → h¹(K'ᵛ) → 0` (`injective_precompModule_cokernelMap`, `ker_precompModule_cokernelMap`, `dualDelta`, `ker_dualDelta`), the first two statements needing no splitting hypothesis. Combining with `shortExact_toCoker` gives the derived-triangle input: a degreewise injective chain map with projective cokernel terms has an exact dual sequence for every coefficient module and every test algebra. For an obstruction theory the sequence degenerates: a degreewise injective obstruction theory is a quasi-isomorphism, so `IsObstructionTheory.acyclic_coker` shows that both `h⁰(coker φ)` and `h¹(coker φ)` vanish, and with a splitting the dual chain map is itself a quasi-isomorphism, upgrading the fully faithful comparison of `IsObstructionTheory.isCohomologicalMono_dualComplexHom` to an equivalence of the dual Picard groupoids. Missing: naturality of the dual six-term sequence in the coefficients and in the test algebra, so the exact sequence is not yet stated as a sequence of prestacks. **Cone quotients as torsor prestacks (`Cones/QuotientTorsor.lean`).** For an affine cone with a vector-bundle action `A : ConeAction R S F` and a basis `b` of `F` indexed by `σ`, the bundle is realised as a group algebraic space `vectorBundleGroup σ` and the cone as `coneActionSpace A b : AlgebraicSpaceAction`, with all group and action axioms proved (via `GrpObj.ofRepresentableBy` for the representable presheaf of groups `X ↦ (σ → Γ(X,⊤))`, and `actOn_zero`/`actOn_add` deduced from the `ConeAction` axioms). Because `AlgebraicSpaceGroup` is an absolute group object in fppf sheaves, the relative bundle `E = Spec Sym_R F` cannot be used directly; the ℤ-model `𝔾ₐ^σ = Spec ℤ[xᵢ]`, which base-changes to `E` over `Spec R` and has the same points over every `R`-algebra, is used instead. The comparison `trivialTorsorFunctor A b : [C/E](B) ⥤ ActionTorsor …(Spec B)` sends a point to the trivial torsor with target `(e,s) ↦ e·x(s)` and a translation to the corresponding translation; it is proved `Full` and `Faithful` (`trivialTorsorFunctorFullyFaithful`), the key input being `eq_translationMap`: every equivariant self-map of the trivial torsor over the base is a translation. Object-level naturality in the test algebra (`trivialTorsorFunctorNaturality`) compares `mapQuotient` with `ActionTorsor.pullbackFunctor`. `isoTrivialOfSection` proves unconditionally, from `principal_isIso`, that a torsor with a global section is isomorphic to a trivial one; essential surjectivity (`essSurj_trivialTorsorFunctor`) is therefore reduced to two explicit hypotheses, neither of which is proved: `AllTorsorsTrivial` (the vanishing of `H¹(Spec B, E)`) and the requirement that the point of the cone produced by a section lie over the given `R`-algebra structure. The pseudofunctor coherence of the naturality isomorphism and the stack property remain open. **Base change and fibre products of cone stacks (`Cones/StackFibreProducts.lean`).** For `C : ConeStack base O` and `f : StackHom base' base`, `ConeStack.baseChange` builds a cone stack over `base'` whose total stack is the genuine two-pullback `fiberTwoPullbackStack C.projection f` of `Stacks/TwoPullback.lean` (fibres: triples `(x, b, α : projection x ≅ f b)`), whose projection is the second projection of that two-pullback and whose vertex is the lift of `f ≫ C.vertex` produced by the bicategorical universal property `StackTwoPullback.canonicalLift`; its section property `baseChangeVertexProjectionIso` is that universal property's own comparison cell rather than an assumption. The fibrewise contraction `baseChangeContraction` contracts the first component and transports the comparison isomorphism through `contractionProjectionIso`; it lies over the new base on the nose. The same construction applied to the two projections of `C D : ConeStack base O` gives `ConeStack.prod`, together with the two projections `prodHomFst`, `prodHomSnd` as morphisms of cone stacks, which are strictly equivariant for the contractions. The total stacks, projections, vertices, section cells and contraction functors are unconditional; the five contraction coherence isomorphisms require compatibilities of `contractionOneIso`, `contractionMulIso`, `contractionZeroIso`, `contractionVertexIso` and `contractionPullbackIso` with `contractionProjectionIso` which `ConeStack` does not encode, and which are therefore explicit `Prop` hypotheses (`ConeStack.BaseChangeCoherence`, `ConeStack.ProdCoherence`) of the two constructions; no field of the produced cone stacks is assumed. Establishing those coherences (or adding them to `ConeStack` itself) and the pseudofunctoriality of base change remain open. **Coherence of cone stacks (`Cones/StackCoherence.lean`).** `ConeStack.IsCoherent` records the five higher coherence laws of the contraction data that `ConeStack` omits: each of `contractionOneIso`, `contractionMulIso`, `contractionZeroIso`, `contractionVertexIso` and `contractionPullbackIso` must project, through `contractionProjectionIso` — and, for the zero law, the section comparison `vertexProjectionIso`, for the reindexing law the strong-naturality cell `projectionNaturality` of the projection — to the expected comparison of the base. `BaseChangeCoherence.of_isCoherent` and `ProdCoherence.of_isCoherent` prove that these five laws imply all ten coherence hypotheses of `ConeStack.baseChange` and `ConeStack.prod`, so `baseChangeOfCoherent` and `prodOfCoherent` construct base changes and fibre products of coherent cone stacks with no hypotheses at all; `isCoherent_baseChange` shows every base change is coherent (its projection comparison is an identity), so iterated base changes remain unconditional. The concrete cone stacks of `Cones/Stack.lean` are coherent: `isCoherent_zeroConeStack`, `isCoherent_zeroAbelianConeStack`, `isCoherent_zeroVectorBundleStack`. The file also supplies the missing fibre-component lemmas for `StackIso2.symm`, `associator`, `whiskerLeft`, `whiskerRight`, the natural isomorphism `StackHom.naturalityIso`, and the comparison formulas for the points of the two-pullback constructions. Coherence of a fibre product of coherent cone stacks, and any coherence theorem asserting that all diagrams commute, remain open. |
| 5: intrinsic cones | `IntrinsicNormalCone/*`, `Cones/NormalConeAction.lean`, `Cones/NormalConeDimension.lean`, `Algebra/DimensionFormula.lean`, `Cones/NormalConeProduct.lean`, `Algebra/TensorSubspaceDistrib.lean`, `Cones/NormalConeProductInj.lean`, `Cones/AssociatedGradedGrading.lean`, `Cones/NormalConeProductInj2.lean`, `Cones/RefinementQuotient.lean`, `Cones/EmbeddingIndependence.lean`, `Cones/NormalConeBaseChange.lean`, `Cones/IntrinsicPullbackSequence.lean` |  the honest local-embedding type remains; the former records supplying local cones, descent, purity, abelian hulls, and relative comparisons are inactive; the proper point has a separately constructed global zero intrinsic cone. The affine local-embedding comparison is now proved in `Cones/NormalSheafPicard.lean`: `NormalSheafPicard.bundleQuotientEquivDualPoints` is an isomorphism of categories between the quotient groupoid of `ConeQuotient.bundleTranslationAction d` and the Picard groupoid of the `B`-points of the dual of `[N → F]`, strictly compatible with reindexing (`toDualPoints_naturality`) and with the `𝔾ₘ`-contraction (`contractionFunctor_comp_toDualPoints`), assembled into `NormalSheafPicard.quotientPrestackIsoDualPrestack` in `CommAlgCat A ⥤ Cat`; specialised to the conormal map it gives `NormalSheafPicard.AffineIntrinsicNormalSheaf.normalSheafPrestackIsoDualPrestack` and, through the honest chain-homotopy equivalence `conormalHomotopyEquivalence` with `CotangentComplex.AffinePresentation.twoTerm` of the presentation `R → R/I` (`quotientExtension_ker : ker = I`), the statement `normalSheafQuotientEquivPresentation` that the affine intrinsic normal sheaf `h¹/h⁰(L^∨)` of `R/I` over `k` is `[N_{U/M}/T_M|_U]` over every test algebra, with automorphisms `h⁰` and isomorphism classes `h¹` (`autEquivH0`, `isoClass_eq_iff`); the same file supplies `symProdEquiv : Sym(M × N) ≃ Sym(M) ⊗ Sym(N)`, the algebra input previously recorded as missing for fibre products of cone actions. Vistoli's lemma, the input to the intrinsic normal *cone*, is proved in the polynomial model in `Cones/ConeTranslation.lean`: for `M = 𝔸^σ_A = Spec R` and any ideal `I ⊆ R`, the translation `x ↦ x + ε t⁻¹` of Laurent polynomials preserves the extended Rees algebra (`ConeTranslation.translate_mem`, from the Taylor expansion `exists_translateLaurent_eq`), descends to the special fibre `gr_I(R)` of the deformation space and, through flat base change of the associated graded ring along `R → R[ε]`, yields the `R`-algebra coaction `ConeTranslation.coaction : gr_I(R) → gr_I(R)[ε_σ]` of the tangent bundle `T_M|_U = U × 𝔸^σ` on the affine normal cone, computed on degree-one classes as `[f t] ↦ [f t] + Σ_i ε_i [∂_i f]` (`coaction_degreeOne`) and proved to satisfy the counit and coassociativity laws (`coaction_counit`, `coaction_coassoc`; on `B`-points `translatePoint_zero`, `translatePoint_add`); the closed immersion `C_{U/M} → N_{U/M}` is proved equivariant, the coaction being compatible with the translation coaction `nsCoaction` of the normal sheaf `Spec Sym(I)/I·Sym(I)` through `nsToGr` (`coaction_comp_nsToGr`), which is exactly the statement that the tangent action on the normal sheaf preserves the normal cone; the action is packaged as the morphism of schemes `ConeTranslation.actionMap : C_{U/M} × 𝔸^σ → C_{U/M}`. What remains is the fppf stackification of these prestacks, the non-affine base, the passage from the naive presentation complex to the actual `L_X` of a stack, and Vistoli's lemma for a general smooth ambient `M` (only the ambient affine space, the local model, is treated) (Round 12.) **The tangent action on the affine normal cone as a cone action** (`Cones/NormalConeAction.lean`). The polynomial-model coaction `ConeTranslation.coaction : gr_I(R) →ₐ[R] gr_I(R)[ε_σ]` is packaged as an honest `ConeQuotient.ConeAction (R/I) (gr_I(R)) ((R/I) ⊗_R Ω[R⁄A])` — `NormalConeAction.normalConeAction` — for `R = A[x_i]`; the bundle is literally that of `AffineNormalCone.normalSheafTangentAction`, the `𝔸¹`-contraction is `GradedCone.normalConeCoactionQuot`, and all four `ConeAction` axioms are theorems: the unit and associativity laws come from `ConeTranslation.translatePoint_zero`/`translatePoint_add` through `NormalConeAction.translate_act`, and the `𝔾ₘ`-equivariance `NormalConeAction.conic_act` is deduced from that of the normal sheaf using surjectivity of `Sym_{R/I}(I/I²) ↠ gr_I(R)`. The bridge is `NormalConeAction.tensorEquiv`, built from `SymmetricAlgebra.equivMvPolynomial` for the basis `NormalConeAction.tangentBasis` of coordinate differentials and `MvPolynomial.algebraTensorAlgEquiv`. The mathematical core is `NormalConeAction.tangentToPoly_conormalMap`: the conormal map `I/I² → (R/I) ⊗ Ω` is the Taylor derivation `f ↦ Σ_i ε_i ∂_i f`, proved by uniqueness of derivations of a polynomial ring; from it `NormalConeAction.isEquivariant_nsToGrAlg` gives Vistoli's lemma in cone-action form, namely that the closed immersion `C_{U/M} ↪ N_{U/M}` is equivariant. Consequently `NormalConeAction.normalConeObstructionCone` instantiates `PicardCriteria.obstructionCone` unconditionally with the affine intrinsic normal cone and is fibrewise fully faithful and injective on isomorphism classes; `NormalConeAction.isEquivalence_quotientFunctor_of_bijective` shows `[C/T](B) ≌ [N/T](B)` whenever `Sym(I/I²) → gr_I(R)` is bijective (the lci case of `Cones/RegularSequence.lean`); and when `I/I² = 0`, in particular for `I = ⊥`, `NormalConeAction.nonempty_hom_of_subsingleton` and `NormalConeAction.autEquivTangent_of_subsingleton` identify `[C/T](B)` with the classifying groupoid of `T_M(B)`. What remains missing is the passage from this affine polynomial model to a general ambient smooth scheme and the global gluing of the resulting cone stacks. **Purity of the affine normal cone (`Cones/NormalConeDimension.lean`).** For a field `k`, a finitely generated `k`-domain `R` with `ringKrullDim R = n`, and a proper ideal `I`, the extended Rees algebra `A = R[It, t⁻¹]` is proved to be a Noetherian domain of finite type over `k` (`fg_extendedRees`, `finiteType_extendedRees`, `isNoetherianRing_extendedRees`) of Krull dimension `n + 1` (`ringKrullDim_extendedRees`, via `R[X] ↪ A ↪ R[t,t⁻¹]` and transcendence degrees). The parameter `t⁻¹` is a nonzerodivisor and is a non-unit whenever `I ≠ ⊤`, and every prime of `A` minimal over it has height exactly one (`height_eq_one_of_mem_minimalPrimes_parameterIdeal`, Krull's principal ideal theorem). Since `A/(t⁻¹) ≅ gr_I(R)`, this gives unconditionally `dim gr_I(R) ≤ n` and `dim (gr_I(R) ⧸ q) ≤ n` for every ideal `q` (`ringKrullDim_associatedGradedRing_le`, `ringKrullDim_quotient_le_of_ideal`), and unconditionally `dim gr_⊥(R) = dim R`. Granting the classical dimension formula `dim (A ⧸ p) + ht p = dim A` — stated as the explicit hypothesis `HasDimensionFormula`, since Mathlib does not yet know that finite type algebras over a field are catenary — every minimal prime `q` of `gr_I(R)` satisfies `dim (gr_I(R) ⧸ q) = n` (`ringKrullDim_quotient_eq_of_mem_minimalPrimes`), `dim gr_I(R) = n` (`ringKrullDim_associatedGradedRing`), and `Spec (gr_I R)` is pure of dimension `n` in the repository's sense (`pureTopologicalDimension_normalCone : Curves.PureTopologicalDimension n (AffineNormalCone.scheme R I)`). This is the affine algebraic form of Fulton B.6.6, the input to Behrend–Fantechi's statement that the intrinsic normal cone is pure of dimension zero. What remains missing is the dimension formula itself, and the globalisation from `Spec (gr_I R)` to the quotient stack `[C_{U/M}/T_M|_U]`. **The dimension formula for finite type domains over a field (`Algebra/DimensionFormula.lean`).** `ringKrullDim_quotient_add_height` proves, unconditionally, that for a field `k`, a finitely generated `k`-algebra `A` which is a domain and a prime `p`, `dim (A ⧸ p) + ht p = dim A` (Stacks 02JX, Matsumura 5.6). The proof is in three steps: `le_ringKrullDim_quotient_span_singleton` shows that cutting a polynomial ring in `s` variables by one nonzero non-unit equation leaves dimension at least `s - 1`, by presenting the ring as `D[X]` in a variable occurring in the equation and observing that `D` injects into the quotient; `ringKrullDim_quotient_succ_eq_of_height_eq_one` handles a height-one prime `P` of a finite type `k`-domain `B` by Noether normalization, using Mathlib's going-down for integral extensions of integrally closed domains to see that the contraction of `P` to the polynomial ring again has height one, hence is principal; `ringKrullDim_quotient_add_height_aux` then treats a general prime by induction along a chain realising its height, each step of which is saturated. As a consequence `hasDimensionFormula` discharges the `HasDimensionFormula` hypothesis of `Cones/NormalConeDimension.lean`, so `ringKrullDim_associatedGradedRing'` and `pureTopologicalDimension_normalCone'` give unconditionally that the affine normal cone `Spec (gr_I R)` of a proper ideal of an `n`-dimensional finitely generated domain over a field is pure of dimension `n` — the affine algebraic form of Fulton B.6.6 used by Behrend–Fantechi. What remains is the globalisation from the affine normal cone to the quotient stack `[C_{U/M}/T_M|_U]`. **Product formula for the affine normal cone (`Cones/NormalConeProduct.lean`).** For `k`-algebras `R`, `R'` and ideals `I`, `I'`, `productIdeal` is the ideal `J = I ⊗ R' + R ⊗ I'` of `U × U'` inside `M × M'`, and `grTensorToGr : gr_I(R) ⊗[k] gr_{I'}(R') →ₐ[k] gr_J(R ⊗[k] R')` is the canonical comparison map, built from the two maps of Rees algebras `reesLeftAlg`, `reesRightAlg` induced by the inclusions of the factors. `grTensorToGr_surjective` proves unconditionally that it is surjective — so `C_{U×U'/M×M'} → C_{U/M} × C_{U'/M'}` is a closed immersion — via `adjoin_productGenerators`, which shows that `Rees_J` is generated over `R ⊗[k] R'` by the degree-one monomials of the generators `I ⊗ 1` and `1 ⊗ I'`. `grTensorToGr_degreeOne_left` and `grTensorToGr_degreeOne_right` record compatibility with the canonical degree-one generators `conormalToAssociatedGraded`. The isomorphism `C_{U×U'/M×M'} ≅ C_{U/M} × C_{U'/M'}` (`grTensorEquiv`) and the resulting dimension equality are stated with the injectivity of `grTensorToGr` as an explicit hypothesis: over a field it follows from `J^n/J^{n+1} ≅ ⊕_{a+b=n} (I^a/I^{a+1}) ⊗_k (I'^b/I'^{b+1})`, whose distributivity input (`(A ⊗ R') ∩ (R ⊗ B) = A ⊗ B` for subspaces) is not in Mathlib and is not formalised here. Still missing: that injectivity; the normal sheaf analogue `N_{U×U'/M×M'} ≅ N_{U/M} × N_{U'/M'}`, which needs the same input plus base change for symmetric algebras on top of `NormalSheafPicard.symProdEquiv`; and `dim(A ⊗_k B) = dim A + dim B` for finite type `k`-domains, needed to turn the isomorphism into `dim C_{U×U'/M×M'} = dim R + dim R'`. **Distributivity for tensor products of subspaces (`Algebra/TensorSubspaceDistrib.lean`) and the product-ideal filtration (`Cones/NormalConeProductInj.lean`).** Writing `tensorSub A B` for the image of `A ⊗[k] B` in `V ⊗[k] W`, `tensorSub_top_inf_top_tensorSub` proves the distributivity law `(A ⊗ W) ⊓ (V ⊗ B) = A ⊗ B` over a field, without bases: over a field every subspace is the range of an idempotent endomorphism, `range (e ⊗ f) = A ⊗' B`, and membership in `A ⊗' B` is the fixed-point condition `(e ⊗ f)x = x`, so the statement follows from `(e ⊗ 1) ∘ (1 ⊗ f) = e ⊗ f`. On the ideal side, `Ideal.sup_pow_eq_sum` gives `(I ⊔ J)^n = Σ_{a ≤ n} I^a J^{n-a}` in any commutative ring (the binomial expansion in the idempotent semiring of ideals) and `productIdeal_pow_eq` specialises it to `J^n = Σ_{a+b=n} I^a I'^b` for the ideal of `U × U'`, upgrading the earlier one-sided bound. These are the two inputs to the identification `J^n/J^{n+1} ≅ ⊕_{a+b=n} (I^a/I^{a+1}) ⊗_k (I'^b/I'^{b+1})`. That identification, and hence the injectivity hypothesis of `grTensorEquiv`, is still open: it additionally needs `(A₁ ⊓ A₂) ⊗' ⊤ = A₁ ⊗' ⊤ ⊓ A₂ ⊗' ⊤` (flatness of the second factor) and a splitting of the finite part of the `I`-adic filtration, and the passage from degreewise to global injectivity needs a grading on `AffineNormalCone.associatedGradedRing`, which the repository does not yet carry. **The grading of the affine normal cone (`Cones/AssociatedGradedGrading.lean`).** `mem_reesIdeal_iff` proves that the ideal `I · Rees_I(R)` defining `AffineNormalCone.associatedGradedRing R I` is homogeneous, by combining the two existing coefficient lemmas of `Cones/Affine.lean` and `Cones/DeformationSpace.lean` into the description `q ∈ I · Rees_I(R) ↔ ∀ m, coeff m q ∈ I^{m+1}`. Polynomial truncation then descends to a family of `R`-linear projections `grProj R I n` on `gr_I(R)` which are idempotent (`grProj_idem`), pairwise orthogonal (`grProj_comp_of_ne`) and sum locally finitely to the identity (`sum_grProj`), with homogeneous parts `grPiece R I n`. The resulting `injective_of_degreewise` is the tool for deducing injectivity of a comparison map from injectivity in each degree. Two consequences are recorded: `associatedGradedBaseRingHom_injective` (the degree-zero part is exactly `R ⧸ I`) and `conormalToAssociatedGraded_injective` (the degree-one part is exactly the conormal module `I/I²`, so the comparison `N_{U/M} → C_{U/M}` is injective in degree one). The grading is given as orthogonal projections rather than as a Mathlib `GradedAlgebra` instance: Mathlib has no grading on `Polynomial`, none on `reesAlgebra`, and no graded structure on a quotient by a homogeneous ideal, and a `DirectSum.Decomposition` would require a non-canonical degree bound on quotient elements. Still open, and now the only obstruction to `grTensorEquiv` becoming unconditional: the lattice identity `(A₁ ⊓ A₂) ⊗' ⊤ = A₁ ⊗' ⊤ ⊓ A₂ ⊗' ⊤` (available in Mathlib in principle through `Module.Flat.rTensor_exact`), the degreewise isomorphism `J^n/J^{n+1} ≅ ⊕_{a+b=n} (I^a/I^{a+1}) ⊗_k (I'^b/I'^{b+1})`, and the induced total grading on `gr_I(R) ⊗[k] gr_{I'}(R')`. **The lattice of tensor products of subspaces, completed (`Cones/NormalConeProductInj2.lean`).** Over a field, `ker_rTensor_eq` proves `ker (f ⊗ 1) = (ker f) ⊗' ⊤` for every linear map `f`, by left exactness of `- ⊗ W` (`Module.Flat.rTensor_exact` with `LinearMap.exact_subtype_ker_map`), together with the bridging lemma `tensorSub_top_eq_range_rTensor` and the generalisation `range_tensorMap'` of the earlier range computation to arbitrary linear maps. Applying it to `(A₁.mkQ).prod (A₂.mkQ)` and the canonical isomorphism `(P × Q) ⊗ W ≅ (P ⊗ W) × (Q ⊗ W)` gives the one-sided distributivity law `inf_tensorSub_top : (A₁ ⊗' ⊤) ⊓ (A₂ ⊗' ⊤) = (A₁ ⊓ A₂) ⊗' ⊤`, its right-hand mirror `inf_top_tensorSub`, and, combined with `tensorSub_top_inf_top_tensorSub`, the two-sided law `inf_tensorSub : (A₁ ⊗' B₁) ⊓ (A₂ ⊗' B₂) = (A₁ ⊓ A₂) ⊗' (B₁ ⊓ B₂)`. Binary meets in the lattice of tensor products of subspaces are therefore fully computed. What remains for the injectivity of `grTensorToGr` is the distributivity of `⊓` over the *finite sums* `Σ_{a+b=n} I^a ⊗' I'^b`, which does not follow from binary meets and is best obtained by splitting the finite part of the `I`-adic filtration into complements `I^a = I^(a+1) ⊕ C_a`, and then the total-degree projections on `gr_I(R) ⊗[k] gr_{I'}(R')` intertwining with `AffineNormalCone.grProj`, at which point `AffineNormalCone.injective_of_degreewise` applies. Accordingly `grTensorEquiv` still carries its injectivity hypothesis. `Cones/RefinementQuotient.lean` proves the refinement lemma for local embeddings in the polynomial model, the compatibility that lets the local presentations `[C_{U/M}/T_M|_U]` glue. For an arbitrary ring `R`, ideal `I` and variable set `τ`, the ideal `polyExt τ I = I·R[y_τ] + (y_τ)` of the zero-section embedding has all its powers computed coefficientwise (`pow_eq_coeffFiltration`: `f ∈ (I')^N` iff `coeff m f ∈ I^(N − |m|)` for every `y`-monomial `m`), and from this the comparison map `prodMap : gr_I(R)[z_τ] → gr_{I'}(R[y_τ])`, sending `z_t` to the degree-one class of the new coordinate `y_t`, is proved surjective (`prodMap_surjective`, the degree-one class of a monomial of `y`-degree at least two vanishing) and injective (`prodMap_injective`, by lifting the coefficients to the Rees algebra and reading off the coefficientwise description `mem_map_rees_iff` of `I·Rees(I)`), giving the product formula `prodEquiv`, i.e. `C_{U/M'} = C_{U/M} × 𝔸^τ`, with no noetherian, regularity or flatness hypothesis. In the polynomial model `M = 𝔸^σ_A`, `M' = 𝔸^{σ⊕τ}_A` the ideal of the graph embedding `x ↦ (x, g(x))` is `graphIdeal A σ τ I g`, proved to present the same subscheme (`graphQuotientEquiv : R'/I' ≃ₐ[A] R/I`, both directions explicit), to equal `I·R' + (y_τ)` after the change of variables `y_t ↦ y_t + g_t` (`map_shear_graphIdeal`, `graphIdeal_zero_eq`), and `refinementEquiv` transports `prodEquiv` along `sumRingEquiv : A[x_σ,y_τ] ≃ (A[x_σ])[y_τ]`, computed on the degree-zero, degree-one and new-coordinate generators. The tangent translation action of `Cones/ConeTranslation.lean` respects this decomposition on `B`-points (`translatePoint_comp_refinementEquiv`, using the compatibility `taylor_incl` of the Taylor derivation with the inclusion of variables): the `𝔸^{σ⊕τ}`-action is the product of the `𝔸^σ`-action on `C_{U/M}` and the translation action of `𝔸^τ` on itself. The gate statement is `refinementQuotientEquivalence`: for every test algebra `B` the functor `refinementFunctor` between the action groupoids `ConeGroupoid` of `translatePoint` — i.e. `[C_{U/M'}/T_{M'}|_U](B) → [C_{U/M}/T_M|_U](B)` — is fully faithful and essentially surjective, so the quotient presentation of the intrinsic normal cone is unchanged by the refinement. For the normal sheaves only half is available: `nsProdMap : N_{U/M}[z_τ] → N_{U/M'}` is constructed, proved surjective (`nsProdMap_surjective`) and proved compatible with the closed immersions `C ⊆ N` (`nsToGr_comp_nsProdMap`), but its injectivity, hence `N_{U/M'} = N_{U/M} × 𝔸^τ` and the corresponding equivalence of quotient groupoids, is not proved; also missing are a general smooth `M' → M` (only the projection `𝔸^{σ⊕τ} → 𝔸^σ` with a graph embedding is treated) and any scheme-level or non-affine statement. `Cones/EmbeddingIndependence.lean` completes the affine gate: a lift `h` of one presentation `A[x_{σ'}] ↠ S` into another `A[x_σ] ↠ S` induces `presMap : gr_{I'} → gr_I` and the comparison functor `compareFunctor` of quotient groupoids, whose action on tangent vectors is the transpose Jacobian `compareVec` and whose well-definedness is the chain rule `taylor_liftHom` (`translatePoint_comp_presMap`); the comparisons compose strictly (`compareFunctorCompIso`), the identity lift gives the identity (`compareFunctorIdIso`), and two lifts of the same presentation give canonically isomorphic functors (`liftIso`, from `point_degreeOneRaw_sub`: the degree-one class of `h'(f) − h(f)` is the Taylor image of the classes `[h' t − h t]`), whence `presentationEquivalence` (two presentations have equivalent quotient groupoids over every test algebra) and `cocycleIso` (the Behrend–Fantechi cocycle condition for three presentations); `ker_jointPres` identifies the common refinement of two embeddings with the graph embedding of `RefinementQuotient`, and `nsToGr_comp_nsPresMap`, `point_comp_nsToGr`, `nsTranslatePoint_comp_nsPresMap` prove the comparison compatible with the closed immersions `C ⊆ N` and with the tangent translation on the normal sheaf. Still missing: the injectivity of `nsProdMap` (the product formula for normal *sheaves*, hence the normal-sheaf analogue of `refinementQuotientEquivalence`), a general smooth ambient `M' → M` (only the projection `𝔸^{σ⊕τ} → 𝔸^σ` and graph embeddings are treated), and any non-affine base or stackification. **Scalar extension of the affine normal cone and of its tangent action** (`Cones/NormalConeBaseChange.lean`). For a flat extension of coefficients `A → A'` — the case of a field extension `k ⊆ k'` — with `R = A[x_σ]`, `R' = A'[x_σ]` and `I' = I R'`, the affine local model of the intrinsic normal cone is unchanged. `NormalConeBaseChange.flat_mvPolynomial` lifts flatness to the polynomial rings and `NormalConeBaseChange.grBaseChangeEquiv` identifies `gr_{I'}(R')` with `R' ⊗_R gr_I(R)` (an instance of `AffineNormalCone.grTensorEquiv`), compatibly with the degree-zero and degree-one generators. The tangent action is unchanged: `NormalConeBaseChange.taylor_map` shows the Taylor derivation commutes with coefficient extension, whence `NormalConeBaseChange.coaction_comp_grMap` for the coaction of `Cones/ConeTranslation.lean` and `NormalConeBaseChange.translatePoint_grMap` for the action on `B`-points. At the level of the quotient groupoids of `Cones/NormalConeAction.lean`, `NormalConeBaseChange.pointEquiv` is the bijection of objects coming from the universal property of the base change and `NormalConeBaseChange.pointEquiv_translatePoint` shows it intertwines the actions, while `NormalConeBaseChange.translatePoint_of_hom` and `homOfTranslatePoint` describe the arrows of either groupoid as tangent vectors `σ → B` with the same translation law. Finally, for `σ` finite over a field, `NormalConeBaseChange.finrank_tangent` and `NormalConeBaseChange.ringKrullDim_gr` give `rank T_M|_U = #σ = dim gr_I(R)` for a proper ideal, so `NormalConeBaseChange.normalCone_dim_sub_rank_eq_zero` records that the two integers attached to the presentation `[C_{U/M}/T_M|_U]` differ by zero. Still missing: the packaging of the object and arrow bijections into an equivalence of quotient groupoids (which needs the `R/I → R'/I'` algebra plumbing), and the compatibility of the scalar extension with the closed immersions `Sym(I/I²) ↠ gr_I(R)`. `Cones/IntrinsicPullbackSequence.lean` builds Behrend–Fantechi's intrinsic pullback sequence in the affine model of a tower `R → S → T` presented by Mathlib generators `Q : Generators S T`, `P : Generators R S`. The chain-level Jacobi–Zariski triangle is realized as two chain maps `jzToComp`, `jzOfComp` between the presentation complexes `T ⊗_S [I/I² → Ω]`, `[J/J² → Ω]` and `[K/K² → Ω]`, whose degreewise exactness (middle exactness in both degrees, surjectivity on the right, injectivity on the left in degree `0`) is transported unconditionally from Mathlib's `Algebra.Generators.Cotangent.exact`, `CotangentSpace.exact`, `map_ofComp_surjective` and `map_toComp_injective`. Adding the single hypothesis that `T ⊗_S I/I² → J/J²` is injective, `shortExact_jz` turns the triangle into a `ShortExact` sequence of two-term complexes, from which the six-term Jacobi–Zariski cohomology sequence follows (`jzDelta`, `jz_exact_h0_middle`, `jz_exact_h0_right`, `jz_exact_h1_left`, `jz_exact_h1_middle`, `jz_surjective_h1`). When the relative conormal module `K/K²` is projective — the local complete intersection condition, the degree-`0` splitting being automatic since `Ω` of a presentation is free — `isDegreewiseSplit_jz` makes the sequence degreewise split, and `shortExact_dual_jz`/`shortExactPicard_dual_jz` dualise it: over every test algebra `B` the sequence `h¹/h⁰(L_{T/S}ᵛ)(B) → 𝔑_{T/R}(B) → (𝔑_{S/R} ×_{Spec S} Spec T)(B)` is exact in the Layer-4 sense, with `𝔑_{T/R}(B) → (𝔑_{S/R} ×_{Spec S} Spec T)(B)` essentially surjective, hom-fibres torsors under `h⁰(L_{T/S}ᵛ)(B)` and iso-fibres the orbits of the relative Picard groupoid. Finally `dualJzLeftEquivalence` identifies the last term with the intrinsic normal sheaf of `S` over `R` evaluated at `B` as an `S`-algebra, by the base-change adjunction. Missing: the two carried hypotheses are not derived from an lci or flatness assumption on `S → T`, and the comparison with the derived Jacobi–Zariski triangle of `CotangentComplex/Full.lean` is not formalized. |
| 6: obstruction theories | `ObstructionTheory/*`, `ObstructionTheory/AffineObstructionCone.lean`, `ObstructionTheory/DeformationMeaning.lean`, `ObstructionTheory/PointObstruction.lean`, `ObstructionTheory/Curvilinear.lean`, `ObstructionTheory/CurvilinearStrict.lean`, `ObstructionTheory/DerivedInvariance.lean` |  the cohomological definition remains; the point POT is proved perfect from its concrete, field-specific finite-free resolution and zero amplitude; the former arbitrary-predicate resolution, geometric-realization, and deformation packages are inactive. Behrend–Fantechi Theorem 4.5 is now proved in the affine two-term model in `ObstructionTheory/AffineCriterion.lean`: `PicardCriteria.IsObstructionTheory` (`H⁰(φ)` bijective, `H⁻¹(φ)` surjective) is invariant under quasi-isomorphism and chain homotopy equivalence, `IsObstructionTheory.isCohomologicalMono_dualHom` shows it makes `h¹/h⁰(Lᵛ) → h¹/h⁰(Eᵛ)` a fibrewise closed immersion (`PicardCriteria.IsCohomologicalMono`, hence fully faithful and injective on isomorphism classes) over every test algebra with no finiteness or projectivity hypothesis, and `isObstructionTheory_of_trivSqZeroExt` / `isObstructionTheory_iff_forall_isCohomologicalMono` prove the converse using only trivial square-zero extensions as test algebras; the obstruction cone of an abstract cone inside the normal sheaf is `PicardCriteria.obstructionConeFunctor`, proved fully faithful, injective on isomorphism classes, compatible with reindexing of test algebras and with homotopy equivalences of `E`. What remains is the sheaf-level statement (only the fibres over affine test algebras and their strict naturality are available), the instantiation of the abstract cone with the affine normal cone quotient of `Cones/ConeTranslation.lean` and `Cones/NormalSheafPicard.lean`, and the comparison with the derived-category `DerivedObstructionTheory.ObstructionTheory`, which is still unconnected to the two-term criterion (Round 12.) **Obstruction cones, perfectness, sums and base change in the affine two-term model** (`ObstructionTheory/AffineObstructionCone.lean`). The abstract cone of `PicardCriteria.obstructionConeFunctor` is instantiated with honest affine cone quotients: `PicardCriteria.obstructionCone` sends a cone `[C/T](B)` equipped with an equivariant closed immersion into the normal sheaf to `h¹/h⁰(Eᵛ)(B)`, and is proved fully faithful (`obstructionConeFullyFaithful`, via the new `quotientFunctorFullyFaithfulOfSurjective`) and injective on isomorphism classes (`obstructionCone_injective_isoClass`) — the fibrewise meaning of "closed subcone stack" — as well as natural in the test algebra and invariant under homotopy equivalence of `E`. The instantiations with the full normal sheaf (`normalSheafObstructionCone`), with the affine intrinsic normal sheaf of `I ⊆ R` (`affineNormalSheafObstructionCone`) and with the presentation cotangent complex of `R → R/I` (`presentationObstructionCone`) are unconditional. The two-term notion of an obstruction theory is identified with the derived one: `PicardCriteria.isObstructionTheory_iff_derived` and `derivedObstructionTheory` show that `IsObstructionTheory φ` is exactly `H⁰` iso and `H⁻¹` epi for `DerivedCategory.Q.map (toCochainComplexHom φ)`, using an explicit `ShortComplex.LeftHomologyMapData` identifying the cohomology of `E.toCochainComplex` in degrees `0` and `-1` with `h1 E` and `h0 E`. Perfectness in this model is finite freeness of the two terms (`IsPerfectTwoTerm`); `isFiniteFreeComplex_dualPoints` and `virtualRank_dualPoints` show that every fibre `h¹/h⁰(Eᵛ)(B)` is then the Picard groupoid of a two-term complex of finite free `B`-modules of virtual rank `-virtualRank E`, i.e. a vector-bundle stack. Finally `isObstructionTheory_iff_exact_cone` reformulates the notion as right exactness of `E⁻¹ → L⁻¹ ⊕ E⁰ → L⁰ → 0`, whence `isObstructionTheory_sum_iff` (external direct sums, both directions) and `IsObstructionTheory.baseChange` (base change along *any* `R`-algebra, with no flatness hypothesis), and `GlobalTwoTermResolution.sum` gives stability of global two-term resolutions under biproducts in the derived category. Still missing: the tangent action on the affine normal cone of `Cones/ConeTranslation.lean` is built in the polynomial model and is not packaged as a `ConeQuotient.ConeAction`, so the obstruction cone of the normal cone itself is available only through the equivariance hypotheses of `PicardCriteria.obstructionCone`. **Deformation-theoretic meaning of an obstruction theory (Behrend–Fantechi §4), affine model.** `ObstructionTheory/DeformationMeaning.lean` connects the two-term obstruction-theory criterion of `AffineCriterion.lean` with the square-zero deformation theory of `CotangentComplex/SquareZero.lean`. The Picard dual is redone with arbitrary module coefficients (`PicardCriteria.dualComplex`, `dualComplexHom`, `dualModuleH0Equiv`), and `IsObstructionTheory.isCohomologicalMono_dualComplexHom` proves that an obstruction theory dualises to a cohomological monomorphism for *every* coefficient module, with no finiteness or projectivity hypothesis. The groups `ExtOne R M P` and `ExtZero R M P` are identified on the nose with `h¹` and `h⁰` of that dual (`extOneEquivDualHOne`, `extZeroEquivDualHZero`, `obstructionGroupEquivDualHOne`), which lets the obstruction class be pushed to `obstructionE M P φ ∈ ExtOneE M E`. The two main theorems are `obstructionE_eq_zero_iff` — the obstruction of `E` vanishes exactly when the square-zero lifting problem for `A` is solvable — and `exists_unique_extZeroEVAdd` together with `liftEquivExtZeroE`: the solutions form a simply transitive set under `ExtZeroE M E ≃ Der_R(A, M)`, with `liftIsoClassEquivExtZeroE` describing the isomorphism classes of the lifting groupoid. `obstructionE_push` is naturality along a morphism of square-zero extensions. Finally the obstruction is realised as an *object* `obstructionObject M P φ F` of the cone groupoid `h¹/h⁰(Eᵛ)` whose isomorphism class is `obstructionE`, and `liftEquivTrivialisation` (with the equivariance `trivialisationOfLift_extZeroEVAdd`) proves Behrend–Fantechi's statement that the lifting groupoid is the torsor of trivialisations of that object; the splitting `derSection` of `restrictDer` makes this constructive. Missing: naturality of `obstructionE` under flat base change (only the `Push` version is proved), and the globalisation of all of this beyond the affine presentation model. **Obstruction space at a geometric point (Layer 6, final bullet).** `ObstructionTheory/PointObstruction.lean` completes the affine deformation picture of `DeformationMeaning.lean`. `obstructionE_baseChange` supplies the base-change naturality of the obstruction class of `E` along `R → R'` (via `SquareZeroExt.basePush`, over a fixed `A`; the genuine change of `A` to `R' ⊗ A` is not treated). For a point `x : A → κ` with values in a field, `fibreComplex` is the fibre `κ ⊗[A] E` and `ObSpace`/`TanSpace` are `h¹`/`h⁰` of its `κ`-dual; `finite_obSpace` shows `Ob(x)` is finite dimensional when the terms of `E` are finite. The main theorem `extOneEquivObTensor` proves that for every coefficient module `N` which is a `κ`-vector space, `h¹` of the dual complex of `E` with coefficients in `N` — where the obstruction lives — is `Ob(x) ⊗[κ] N`, so a single finite-dimensional space receives the obstruction of every square-zero deformation problem over the point; `obSpaceEquiv` identifies `Ob(x)` with `h¹(dualComplex E κ)` and `h0DualEquivDerivation` identifies the degree-zero part with `Der_R(A, N)`, in particular the tangent space with `Der_R(A, κ)`. `pointObstruction` and `pointObstruction_eq_zero_iff` give the obstruction in `Ob(x) ⊗[κ] M` and its vanishing criterion. For extensions with a one-dimensional kernel, `curvilinearObstruction` is a single element of `Ob(x)`, homogeneous under rescaling the trivialisation (`curvilinearObstruction_smulOfUnit`), and `CurvilinearObstructions` with `smul_mem_curvilinearObstructions` and `zero_mem_curvilinearObstructions` exhibits the set of such obstructions as a `κˣ`-stable cone. Still missing: the truncated-polynomial family `κ[t]/(tⁿ⁺¹) → κ[t]/(tⁿ)` realising the curvilinear extensions, and the identification of that cone with the fibre of the intrinsic normal cone. **Curvilinear extensions (Layer 6).** `ObstructionTheory/Curvilinear.lean` constructs the curvilinear square-zero extensions `κ[t]/(tⁿ⁺¹) → κ[t]/(tⁿ)`: `curvRing`, `curvIdeal`, `isSquareZero_curvIdeal` (needing `n ≥ 1`, carried as `[NeZero n]`), `curvQuotEquiv` identifying the quotient with `κ[t]/(tⁿ)`, and `curvTrivialisation` proving that the kernel `(tⁿ)/(tⁿ⁺¹)` is the `κ`-line spanned by the class of `tⁿ`. `IsJet` is the condition that the `A`-algebra structure of `κ[t]/(tⁿ)` reduces to the point modulo `t`, and `isScalarTowerCoeff` proves its essential consequence: the `A`-action on the kernel factors through the point, which is what puts the obstruction of a jet in `Ob(x) ⊗ κ`; `constIsJet` and `constLift` show the constant jet is a jet and always extends. The file also re-proves, under the correct hypotheses on the coefficients (`[Module κ (Coeff M)]`, `[IsScalarTower A κ (Coeff M)]` instead of `PointObstruction.lean`'s stronger `[Algebra κ (B ⧸ M)]`, `[IsScalarTower A κ (B ⧸ M)]`, which excludes jets of order ≥ 2), the obstruction at a point: `pointObstructionAt`, `curvilinearObstructionAt` and its vanishing criterion and `κˣ`-scaling law, and `CurvilinearObstructionsAt` with `smul_mem_curvilinearObstructionsAt` and `curvilinearObstructions_subset`. Still missing: the set of obstructions of the truncated-polynomial extensions themselves, blocked by a typeclass diamond (two non-reducibly-equal instance paths for `Semiring` and `SMul` on an iterated quotient ring) that prevents `Lift R (curvIdeal κ n) P.Ring` from elaborating; the module docstring records the diagnosis and two ways to fix it. The identification of the curvilinear cone with the fibre of the intrinsic normal cone remains untouched. **The strict curvilinear obstruction cone (Layer 6).** `ObstructionTheory/CurvilinearStrict.lean` completes the curvilinear picture by keeping the ambient ring abstract: `IsCurvilinear κ M` records an isomorphism `B ≃ₐ[κ] κ[t]/(tⁿ⁺¹)` carrying `M` onto `(tⁿ)/(tⁿ⁺¹)`, so that all instances come from the variables as in `CotangentComplex/SquareZero.lean` and the typeclass diamond that blocked `Curvilinear.lean` disappears. The transported results are `isSquareZero_of_isCurvilinear`, `curvilinearTrivialisation` (the kernel is a `κ`-line), `curvilinearQuotEquiv` (`B ⧸ M ≃ₐ[κ] κ[t]/(tⁿ)`) and `curvilinearAug_mul_eq_zero`; `IsJetOver` is the jet condition and `isScalarTower_coeff_of_isJetOver` proves that a jet makes `A` act on the kernel through the point. `CurvilinearObstructions'` is then the set of obstructions of the curvilinear extensions over all orders, curvilinear structures, jets and trivialisations, and it is proved to be contained in `CurvilinearObstructionsAt` (`curvilinearObstructions'_subset`), stable under `κˣ` (`smul_mem_curvilinearObstructions'`, by rescaling the trivialisation — `exists_units_smul_trivialisation` shows this is the `κˣ`-orbit of the canonical trivialisation, whereas reparametrising `t ↦ ct` would only give `n`-th powers) and to contain the origin (`zero_mem_curvilinearObstructions'`, from the constant `1`-jet, which lifts to `κ[t]/(t²)`); together, the strict curvilinear obstructions form a cone in `Ob(x)`. `jetOneEquivTangent` identifies the first-order jets at `x` with `Der_R(A, κ)`. Missing: the identification of this cone with the fibre of the intrinsic normal cone. `ObstructionTheory/DerivedInvariance.lean` settles two Layer-5/6 items. First, being an obstruction theory is invariant under isomorphism of the source in the derived category: `homotopyEquivalenceOfDerivedIso` turns an isomorphism `Q(E) ≅ Q(E')` of the cochain realizations of two-term complexes of finite free modules into a chain homotopy equivalence (K-projectivity, via `exists_homotopyEquiv_of_derivedIso` and `ofHomotopyEquiv`), `nonempty_chainHomotopy_of_derivedIso` upgrades the compatibility with the target `L` to a chain homotopy `φ' ∘ e ≃ φ` using the homotopy uniqueness `nonempty_homotopy_of_Q_map_eq`, and `isObstructionTheory_iff_of_derivedIso` concludes `IsObstructionTheory φ ↔ IsObstructionTheory φ'`; `nonempty_natIso_dualHom_quotientFunctor` and `nonempty_natIso_obstructionCone` show that the induced functors on the dual Picard groupoids, hence the obstruction cones of `Cones/NormalConeAction.lean`, agree up to a natural isomorphism over every test algebra. Second, the hypotheses of the intrinsic pullback sequence are discharged in the smooth case: `injective_jzToComp_degreeZero_of_flat` proves the conormal map `T ⊗_S I/I² → J/J²` injective when `S` is formally smooth over `R` and `T` is flat over `S`, and `projective_cotangent_of_formallySmooth` proves `K/K²` projective when `T` is formally smooth over `S`, so that `shortExact_jz_of_flat` and `shortExactPicard_dual_jz_of_smooth` give the intrinsic pullback sequence with no remaining hypothesis in that case. A caveat recorded in the file: plain flatness of `T` over `S` is not sufficient for the degreewise short exactness — the obstruction is the image of `H₂(L_{T/S})` in `T ⊗ H₁(L_{S/R})`, which is why formal smoothness of `S` over `R` is assumed, and Mathlib contains no injectivity statement for that map. |
| 7: virtual class | `VirtualFundamentalClass/Basic.lean`, `Examples.lean`, `Public.lean` | the proper point has a constructed Rees cone, perfect identity POT, rank-zero target, direct rank-zero resolved formula, virtual/fundamental equality, and degree one; the former general scheme/stack resolved-cone and independence packages are inactive |
| 8: functoriality | `VirtualFundamentalClass/Functoriality.lean` | genuine cartesian-square shapes remain active; the chosen equivalence in a cartesian square is required to classify its displayed cone, and isomorphisms of squares satisfy the comparison-face pasting equation; cotangent-triangle compatibility is absent, and records carrying virtual-pullback, product, and graph formulas as fields are inactive, so the theorems remain to be constructed |
| 9: relative theory | `IntrinsicNormalCone/Relative.lean`, `VirtualFundamentalClass/Relative*.lean` | the relative diagonal used in the relative-DM predicate is constructed as the bilimit lift of the identity cone, but the former relative cone, resolved class, base-change, independence, and formula packages are inactive; genuine relative VFC theory remains to be built |

## Structural guarantees already established

- Stack fibres and quotient fibres are categories with `IsGroupoid`; automorphisms are retained.
- Equivariant fppf torsors over a scheme base change along every scheme morphism: the pulled-back
  sheaf, action, target map, principal isomorphism, and local triviality are constructed, and the
  resulting functors form a genuine pseudofunctor on the locally discrete bicategory of schemes
  with proved unit and associativity coherence.  That pseudofunctor is exported as `[U/G]` and,
  for the one-point algebraic space with its trivial action, as `BG`.  The fibre over each scheme
  is literally the torsor groupoid and base change is literally the constructed pullback functor,
  so no caller supplies stackification or a fibre equivalence.  The trivial torsor `G × T` is
  constructed with its left-multiplication action, its principal isomorphism, inverted using the
  inverse of the group object, and its global section, so every fibre of `BG` is nonempty, and the
  forgetful functor from each `BG` fibre to the groupoid of fppf `G`-torsors is proved to be an
  equivalence that commutes strictly with base change.  Every equivariant map of `G`-spaces
  induces an honest strong transformation of these pseudofunctors, with identity naturality cells
  and all three coherence laws proved.  No stackification of `[U/G]` is claimed: effective fppf
  descent for action torsors, and already the descent of their arrows, remain open and are nowhere
  assumed.
- Schemes embed bicategorically fully faithfully into groupoid-valued fppf stacks through a
  bundled Yoneda pseudofunctor.  Its unitor, compositor, and coherence laws are constructed,
  and on each pair of schemes the discrete category of scheme morphisms is proved equivalent to
  the full hom-category of strong transformations and modifications.
- Composition of representable stack morphisms is derived from the displayed universal
  properties rather than postulated.  The composite universal comparison, nested classifying
  map, projection equation, source-object pullback isomorphism, pseudofunctorial pasting theorem,
  full classification law, and both map and object-isomorphism uniqueness laws are constructed
  and packaged as an actual `StackMorphismPresentation`.  Hence every scheme-morphism property
  stable under composition is proved stable under composition for both the raw presentations
  and their explicit 2-isomorphism-invariant closure.  Complete presentations are now
  transported across an invertible stack 2-cell by conjugating every comparison and deriving
  both uniqueness fields; hence the raw and public predicates are proved equivalent rather
  than merely related by a closure.  The canonical genuine two-pullback projection has a
  constructed presentation over every scheme object: its representing map is reused from the
  original morphism, while its pullback object, face equation, map uniqueness, and
  stabilizer-sensitive object-isomorphism uniqueness are proved.  Thus representable
  properties are stable under this canonical two-categorical base change.  Any two `Genuine`
  bilimit presentations are now compared by universal lifts in both directions; the comparison
  face for each round trip is proved by pasting the two bilimit face equations, and both inverse
  2-cells are derived from bilimit uniqueness.  Composing this constructed equivalence with the
  canonical projection proves arbitrary genuine base-change stability for multiplicative
  representable scheme-morphism properties.  Pre- and postcomposition by a stack equivalence are
  separately proved iff-invariant, so a compatible equivalence of both source and target also
  preserves every multiplicative representable property.
- Binary products of algebraic spaces are constructed internally: the underlying fppf sheaf is
  the categorical product, its diagonal is proved representable by factoring maps from schemes
  through the represented scheme diagonal, and the product of two chosen scheme atlases is
  proved representable, etale, and surjective by two explicit cartesian base changes.  The
  induced cone satisfies the categorical product universal property in algebraic spaces.
- Arbitrary pullbacks of algebraic spaces are likewise constructed as sheaf pullbacks.  The
  canonical map `X ×_Z Y → X × Y` is proved to be the base change of the actual diagonal
  of `Z`; a general constructor proves that the source of any representable morphism to an
  algebraic space is algebraic by deriving both its diagonal and its pulled-back atlas.  The two
  sheaf projections then satisfy the pullback universal property in the induced category.
- Algebraic spaces embed bicategorically fully faithfully into groupoid-valued fppf stacks:
  the embedding is a bundled pseudofunctor with constructed unitor and compositor and proved
  coherence laws.  On every pair of objects, its local hom functor from the discrete category of
  algebraic-space morphisms to the full category of strong transformations and modifications is
  a constructed equivalence.  In particular, every strong transformation is reconstructed from
  a unique sheaf morphism, with an explicit invertible counit modification.  The defining scheme
  atlas of an algebraic space is converted
  into a stack chart by using the actual sheaf pullback square; every alternative chart
  presentation is proved isomorphic to that pullback before geometric properties are
  transferred.  The isomorphism scheme of two objects is similarly constructed as the actual
  base change of the sheaf diagonal.  Hence the algebraic-stack and Deligne--Mumford-stack
  structures on a discrete algebraic space are derived rather than supplied.
- Scheme charts are strong transformations from represented discrete stacks, so arrow maps and
  pullback coherence are inherited from pseudonaturality rather than weakened to inhabited
  objectwise comparisons.
- The point set and topology of an algebraic stack are no longer caller-selected fields: they
  are constructed as the quotient topology of the internally chosen smooth atlas by the
  equivalence relation generated by the two projections from its represented self-overlap.
  Atlas-independence and the local-ring package needed for divisors remain open.
- A `StackTwoPullback.Genuine` contains both the category-equivalent fibre description and its
  full bicategorical universal property.  Its chosen lift is proved compatible with the cone's
  comparison face, and projection induces a bijection from arbitrary modifications to compatible
  pairs of projected modifications.  Thus both essential surjectivity and hom-category full
  faithfulness are present.  `StackTwoPullback.canonicalGenuine` constructs these witnesses for
  every cospan directly from the fibrewise categorical pullback: its lift retains both legs and
  the cone comparison isomorphism, while uniqueness and the hom-set inverse use
  `CategoricalPullback.mkIso` and `CategoricalPullback.hom_ext`.  The right-identity pullback also
  constructs these witnesses explicitly.
  Interchanging the two legs is now an actual involutive equivalence on the categorical fibres;
  at stack level the swapped presentation, comparison-face equation, lift uniqueness, and
  projected-modification bijection are all derived from the original bilimit witnesses.  The
  left-identity pullback is consequently constructed by symmetry rather than supplied anew.
  VFC and deformation records require this bundled type.
- For an arbitrary cospan, the objectwise categorical pullbacks now form an actual
  groupoid-valued pseudofunctor.  Its two projection strong transformations, their canonical
  comparison modification, the inverse modification, and both cancellation laws are
  constructed from the stored objectwise comparison isomorphisms.  Strong transformations now
  induce actual functors between descent-data categories: their cocycles explicitly conjugate
  by the pseudonaturality cells.  Transport along a vertical composite is related to successive
  transport by a proved natural isomorphism, rather than silently identified.  The projected
  descent data and the comparison induced by the stored fibrewise isomorphisms therefore form
  an actual functor to the categorical two-pullback of descent-data categories.  A componentwise
  inverse functor and explicit unit and counit prove that this functor is an equivalence.  A
  general cospan-induced functor on genuine categorical pullbacks is proved to be an equivalence
  when its three component functors are equivalences.  Strong-transformation pseudonaturality
  then supplies an explicit natural isomorphism identifying the canonical global-to-descent
  functor with that induced pullback functor.  Consequently the fibrewise two-pullback
  pseudofunctor is now exported as an `FppfStack`, with a canonical `StackTwoPullback`
  presentation whose fibre equivalence and compatibility are definitionally identities.
- A cartesian-stack-square witness can no longer pair an unrelated equivalence of underlying
  stacks with the displayed square.  It includes projection 2-cells from that equivalence and a
  proof that they classify the full displayed cone; an isomorphism of such squares also obeys
  the pointwise pasting equation between the four side cells and the comparison faces.
- The relative diagonal in `RelativeDeligneMumfordMorphism` is not caller-selected.  It is the
  bilimit lift of the canonical cone with two identity legs, and its two projection comparisons
  and comparison-face classification are derived from the bilimit property before
  representable unramifiedness is imposed.
- `DeligneMumfordStack` asks only for the defining etale-surjective atlas on an algebraic stack.
  Unramifiedness of its diagonal is no longer a redundant constructor field: the general
  etale-atlas/unramified-diagonal criterion remains open until it is proved.  The represented
  scheme case has a separate constructed proof of diagonal unramifiedness.
- Cone-stack equivalences use invertible modifications of strong transformations and therefore
  commute with pullback in the test scheme.  The former intrinsic-cone refinement and cocycle
  packages are inactive; their construction remains open.
- Cone addition, negation, and vector-bundle-stack actions are functors on the actual groupoid
  fibre over a fixed base object and carry natural comparison isomorphisms under reindexing.
- The action-arrow relation for an abelian cone acting on a cone stack is constructed.  The former
  `ConeQuotientPresentation`, however, supplied the quotient cone and the hard essential
  surjectivity/orbit-hom bijectivity results as fields, so it is inactive.  The former
  `ConeStackBaseChange` and non-universal `AbelianHull` records are inactive for the same reason.
- The three stack sites are different Lean types.  Their structure sheaves are induced from
  the represented regular-function functor, whose fppf sheaf condition is proved.  The
  structure sheaves retain their commutative-ring values; their underlying ring sheaves are
  derived functorially for Mathlib's module API rather than forgetting commutativity in the
  ringed-site data.
- Ringed-site morphisms cannot accept unrelated sheaf/module functors: continuity determines
  sheaf restriction, and the structure-sheaf isomorphism determines module restriction.
- Identity and composition of ringed-site morphisms are constructed from the underlying
  continuous functors and the canonical comparison for iterated sheaf pushforward.  Module
  inverse image is that determined restriction functor, while module pullback is Mathlib's
  actual left adjoint to it.  Inverse-image identity/composition comparisons come from the
  corresponding pushforward comparisons; pullback identity/composition comparisons come from
  uniqueness of left adjoints.  None is accepted as a field.  This does not assume the
  still-missing small-etale/lisse-etale continuity and quasi-coherent comparison theorem.
- The tensor product is constructed by taking the pointwise tensor product of the underlying
  presheaves over that commutative structure presheaf and applying Mathlib's actual module
  sheafification.  The structure sheaf is constructed as its unit, with left/right unit
  comparisons obtained from the pointwise unitors and the sheafification counit; symmetry is
  sheafified from the pointwise tensor symmetry.  The full associativity/coherence package and
  preservation of quasi-coherence and finite-local-free rank remain open.
- Binary direct sums and zero module sheaves are inherited from the actual abelian category of
  sheaves of modules.  Canonical finite-free sheaves of every natural rank are built from
  Mathlib's free sheaf, with explicit local generators, finite-presentation witnesses, and
  bases; finite local freeness transports across actual sheaf isomorphisms and adds under the
  constructed direct sum.  Duals, symmetric/exterior powers, and atlas descent remain open.
- Scheme rational equivalence has no caller-selected generator or divisor map: generators are
  actual integral locally Noetherian closed immersions with nonzero function-field elements.
  Divisor cycles are the unmodified order-of-vanishing cycles pushed forward to the ambient
  scheme.  The scheme fundamental cycle uses the local-ring length at each generic point, so
  nilpotent thickness is retained; coefficient one is recovered for reduced schemes.  The total
  relation space is the span of all such principal divisors, and each graded relation space is
  obtained from the canonical dimension grading.  Mathlib's residue-degree map has been proved
  rational-linear here.  Residue degrees are proved multiplicative under composition.  A proper
  map's specializing property is used to lift strict specialization chains, proving directly
  that closure dimension cannot increase; consequently the actual fibre-sum pushforward is
  functorial on dimension-graded cycles for arbitrary proper scheme morphisms.  For closed
  immersions, residue degree one and exact preservation of closure dimension are derived from the
  stalk map and closed topology; pushforward is proved to carry every principal divisor to its
  composite closed subscheme and hence descends functorially to the rational Chow quotient.
  Flat pullback along an open immersion is now constructed rather than assumed: an open immersion
  is flat of relative dimension zero and its scheme-theoretic fibres are single reduced points, so
  every pullback multiplicity is one and the pullback of a cycle is restriction of coefficients.
  Local finiteness of the restricted support is proved from injectivity of the open embedding, and
  the pullback is proved rational-linear and contravariantly functorial for arbitrary composites
  of open immersions. That it descends through rational equivalence is a theorem, not a field: the
  trace of an integral locally Noetherian closed subscheme on an open subscheme is constructed as
  the actual scheme-theoretic restriction of its closed immersion, integrality and local
  Noetherianity are inherited from a nonempty open of an irreducible scheme, the induced map of
  function fields is the canonical dominant-morphism map, and the order of vanishing is proved
  unchanged because an open immersion preserves the codimension of every point and is etale. Hence
  the restriction of a principal divisor is the principal divisor of the restricted function, or
  zero when the trace is empty, and the canonical span of principal divisors is carried into the
  canonical span. The same statement for an arbitrary open immersion is derived by factoring it
  through its open range, using that flat pullback along an isomorphism is the residue-degree
  pushforward along the inverse. On dimension-graded cycles the pullback requires exactly the
  geometric hypothesis that the two certified gradings agree along the immersion, and it is proved
  functorial both on cycles and on the rational Chow quotient. Extension by zero from an open
  subscheme of a Noetherian scheme is constructed and proved to be an actual section of that
  pullback, so restriction of rational Chow classes to an open subscheme is proved surjective;
  composing the closed-immersion Chow pushforward of a closed subscheme of the complement with
  that restriction is proved to be zero. This is the right-exact part of the localization
  sequence; exactness in the middle is not claimed. The principal divisor of a rational function
  on an integral locally Noetherian scheme is proved to lie in the canonical relation space, so
  its rational Chow class is zero, which is the codimension-one comparison in the form the Chow
  definition uses. The norm/divisor theorem needed to descend an arbitrary proper pushforward to
  Chow groups remains open, and the exact missing input is recorded in the module documentation:
  for a proper surjective morphism of integral locally Noetherian schemes the pushforward of a
  principal divisor must vanish when the image drops dimension and otherwise be the divisor of the
  field norm; Mathlib v4.33.1 supplies neither the norm along a finite extension of function
  fields nor the additivity of `Ring.ord` along a finite local extension on which that theorem
  rests. All
  cycle and Chow quotients used by the virtual-class API have coefficient field `ℚ`.  The active
  stack groundwork stops at fixed geometric cycle carriers and stack rational-function
  generators can no longer be replaced by a caller-selected type: they are actual nonzero
  rational maps to the affine line whose two pullbacks agree on the actual represented
  self-overlap of the internally chosen etale atlas.  The overlap and both etale-surjective
  projections are constructed from the atlas representability clause, with the second obtained
  from a proved involution of the represented self-pullback.  The presentation-free
  statement for every pair of dominant atlas maps defining isomorphic stack objects is now
  derived from the overlap universal property rather than stored in the function.  The conversion
  between function-field values and rational maps uses Mathlib's spreading-out theorem, and the
  constant-one function is constructed and proved to descend.  Pullback of a rational map is
  proved to agree with the canonical dominant-morphism map of function fields by unfolding its
  actual dense-open composition domain.  That canonical map is proved to agree with the
  fraction-field extension from every etale local stalk, and the length-defined order is proved
  etale invariant.  This groundwork is not yet promoted to a Chow quotient: the former
  certificate-based relation space could omit rational functions lacking a supplied dense-image
  certificate and is now inactive.  The integral atlas of a closed DM substack is an existential
  property and is chosen
  internally when a calculation needs it, so changing the witness atlas or its proofs no longer
  changes the raw presentation.  The actual cycle-generator type is the quotient of those raw
  presentations by the equivalence relation generated by stack equivalences compatible with
  the closed immersions into the ambient stack (and their certified dimensions).  Thus replacing
  a closed substack by an equivalent stack model cannot create a second cycle-basis vector.
- A `VectorBundleStack` no longer carries an independent integer rank.  It contains a genuine
  smooth-surjective scheme chart, an actual matrix between finite free modules, and fibre
  equivalences with the associated translation quotient; `stackRank` is defined as the
  difference of those two displayed free ranks.
- Those local fibre equivalences are now required to commute with pullback along every scheme
  map.  The comparison uses a constructed scalar-extension functor that maps both vectors and
  translation arrows entrywise, together with chart pseudonaturality to align the displayed base
  objects.  They also preserve zero, addition, and negation on both objects and arrows.  The
  explicit rank-zero bundle satisfies these laws by construction.
- Every algebraic stack has a constructed zero vector-bundle stack: total space, projection,
  vertex, and contractions are identities; each fixed-base fibre is proved contractible; its
  abelian coherences follow from singleton hom-sets; and its chosen smooth atlas presents it by
  the actual zero matrix `0 → 0`, with an explicit equivalence to the quotient groupoid `[0/0]`.
- Invertible 2-cells now transport actual stack-morphism scheme presentations without changing
  their representing scheme map.  Consequently representable properties and pure relative
  dimension are proved invariant under 2-isomorphism rather than re-supplied as fields.
- The former general scheme and stack homotopy-invariance records are inactive because they
  accepted the central Chow equivalence or its bijectivity as a field.  The proper point instead
  defines its rank-zero zero-section Gysin map directly as transport along the proved equality
  `i + rank(F₁) = i`.  General positive-rank homotopy invariance, projective-bundle Chern theory,
  self-intersection, and refined Gysin remain open.
- The former normal-cone and deformation base-change packages are inactive; they supplied the
  base-changed cones, deformation spaces, and equivalences as fields.
- The affine deformation to the normal cone is now constructed rather than supplied: the extended
  Rees algebra `R[It, t⁻¹]` inside the Laurent polynomials, its parameter `u = t⁻¹` proved a
  nonzerodivisor, the special fibre `R[It, t⁻¹]/(u)` proved isomorphic to `gr_I(R)` through the
  Rees algebra (surjectivity by splitting off the negative-degree part, and the kernel computed
  coefficientwise as `I · Rees_I(R)` in both directions), the localization at `u` proved to be the
  Laurent polynomial ring, the fibre over `u = 1` proved to be `R` by an explicit division by `u −
  1` inside the extended Rees algebra, and flatness over `k[u]` for a base field `k` proved from
  torsion-freeness through the leading-coefficient argument on polynomials. The scheme `M° = Spec
  R[It, t⁻¹]` therefore carries the affine normal cone as a closed subscheme over the origin, the
  trivial family as the open subscheme over `𝔾_m`, and the original scheme as the fibre over `u =
  1`.
- For every affine ideal, the coordinate map from the normal sheaf to the normal cone is now
  constructed by sending `I/I²` to the degree-one Rees classes and extending through the
  symmetric-algebra universal property.  Its surjectivity is proved from Mathlib's theorem that
  the Rees algebra is generated by the monomials `x t`.  The former
  `AffineNormalConeComparison` record—which allowed a caller to provide both this map and its
  surjectivity, as well as the hard regular-sequence isomorphism—has been removed.
- **The regular-sequence comparison is now proved, in the affine case.**  `Algebra/QuasiRegular.lean`
  proves the classical commutative-algebra theorem that a finite weakly regular sequence in
  Mathlib's sense (`RingTheory.Sequence.IsWeaklyRegular`) is quasi-regular: if `x : Fin n → R`
  is such a sequence, `I = span (range x)`, and `F` is a homogeneous polynomial of degree `d`
  with `F(x) ∈ I^(d+1)`, then every coefficient of `F` lies in `I`.  The proof is the classical
  induction on the length of the sequence: the last element is a non-zerodivisor modulo the ideal
  generated by the previous ones, this is bootstrapped to every power of that ideal, the colon
  computation `I^(d+1) : f = I^d` is derived from it, and the degree induction splits off the
  last variable using `MvPolynomial.optionEquivLeft`.  Nothing about associated graded rings is
  assumed anywhere in that file.
  `Cones/RegularSequence.lean` feeds this into the *existing* canonical map.  Given a
  `RegularGenerators R I` — a wrapper carrying only input data: a length, a family
  `Fin length → R`, a proof that it is a weakly regular sequence, and a proof that it spans `I` —
  it proves `normalSheafCoordinateMap_injective`, that the existing
  `AffineNormalCone.normalSheafCoordinateMap R I : Sym_{R/I}(I/I²) →+* gr_I(R)` is injective.
  Combined with the existing surjectivity theorem this gives a genuine `RingEquiv`
  `normalSheafCoordinateEquiv` (packaged also as an `R/I`-algebra isomorphism
  `normalSheafCoordinateAlgEquiv`), whose forward ring homomorphism is proved (by `rfl`) to be
  that very canonical map, and hence `schemeIsoNormalSheaf`, an isomorphism of schemes whose forward
  morphism is proved to be the existing canonical closed immersion `coneToNormalSheaf R I`; in
  particular `IsIso (coneToNormalSheaf R I)`.  The comparison is proved compatible with the
  degree-one conormal generators, the degree-zero `R/I`-structure maps, the vertex augmentations,
  the projections to `Spec(R/I)`, and the `A^1`-scaling actions (the scaling action on `gr_I(R)`
  is constructed here as the descent of the substitution `t ↦ r t` on the Rees algebra).
  **Scope.**  The theorem proved is affine and takes a chosen finite regular generating sequence
  as input; the sequence must be weakly regular in the given order.  It is *not* a statement
  about arbitrary regular immersions of schemes or stacks, and no Zariski/etale-local or
  stack-level version has been constructed.  The length-one acceptance case (a principal ideal
  generated by a non-zerodivisor) and its instantiation at `(t) subset k[t]` are exported as well.
- For the roadmap's strict nodal test `R = k[x,y]/(xy)` at `I=(x,y)`, the element
  `ι(x̄)ι(ȳ)` is proved nonzero in the actual symmetric algebra by evaluating both conormal
  directions on the explicit dual-number tangent vector `(ε,ε)`.  A Rees-algebra computation
  proves that the canonical map sends it to zero because `xy=0` in `R`.  Hence the actual
  canonical map `Sym(I/I²) → gr_I(R)`, not merely a separately declared quotient model, is
  surjective and noninjective.  The same calculation is also instantiated for the split
  square-zero extension `k⊕kε`.
- The former bivariant-class and operation packages are inactive; their compatibility laws await
  geometric constructions of the underlying operations.
- The former general stack fundamental-cycle, proper-pushforward, flat-pullback, degree,
  homotopy-invariance, Chern, refined-Gysin, localization, exterior-product, and bivariant
  records are inactive.  They accepted one or more of the hard existence, descent, bijectivity,
  or compatibility theorems as fields.  General stack intersection operations therefore remain
  missing rather than conditionally exported.
- The scheme identity cases used by the proper point remain constructed: the rank-zero Gysin
  transport and the actual Rees-cone fundamental cycle.  The represented point's identity atlas
  is also proved pure of relative dimension zero.
- The generic cotangent-object record no longer contains a caller-selected type of local
  presentations, and the unused embedding-comparison record with caller-selected presentation
  and refinement types has been removed.  At the affine level, an actual `Algebra.Extension`
  now definitionally determines its two-term cochain complex and derived localization.  The
  proper-point example separately packages that concrete complex in a field-specific resolution
  whose finite/free predicate and rank are fixed.  Constructing the analogous small-etale
  cotangent object and its comparisons for every actual stack chart remains open.
- The former derived-Picard, intrinsic-cone descent, obstruction geometric-realization, resolved
  stack-cone, and resolution-independence structures are inactive.  Each supplied objects or
  hard comparison theorems that must instead be constructed.  The same is true of the relative
  and functoriality packages.
- For the point, perfectness is a fixed predicate defined by possession of an actual finite-free
  two-term resolution.  The displayed zero resolution proves perfectness, and its derived
  isomorphism to zero proves amplitude `[-1,0]`.  The rank-zero vector-bundle target `[0/0]`, zero
  intrinsic normal sheaf, zero intrinsic cone, identity normal map, closedness, and cone-stack
  comparisons are all constructed directly.
- The former auxiliary scheme-level `ResolvedCone` record is inactive: even after its arbitrary
  Chow-class field was removed, it still accepted the hard homotopy-invariance equivalence as
  data.  The proper-point path instead defines the actual integral Rees cone, its pushed
  fundamental cycle, its quotient Chow class, the ranks of the concrete resolution, and the
  rank-zero Gysin transport separately.  The pushed cone cycle is proved equal to the ordinary
  point fundamental cycle before the Gysin calculation is made.
- `Public.lean` contains no caller-fillable absolute or relative input package.  It exports the
  fully constructed proper-point virtual/fundamental equality and degree-one theorem; general
  entry points will return only after their constructions exist.

## Verification

Run `lake build`.  CI also rejects `sorry`, `admit`, custom `axiom` declarations, and
`native_decide` in project Lean sources.  These checks establish elaboration and the absence of
those explicit placeholders only; they do not show that hard geometric theorems have not been
stored as structure fields.

## Constructed acceptance-test fragments

- `Cones.Examples.NodalNormalCone` constructs the roadmap's node and origin ideal, its Rees
  normal cone and conormal normal sheaf, and a dual-number tangent evaluation.  It proves the
  actual canonical coordinate map `Sym(I/I²) → gr_I(R)` is surjective but not injective by
  exhibiting and independently detecting the nonzero kernel element `ι(x̄)ι(ȳ)`.  Thus the
  canonical normal cone is a proper closed subcone of the normal sheaf without assuming a
  presentation comparison theorem.  `DualNumberNormalCone` supplies a second square-zero test
  of the same reusable canonical-map theorem.
- `Cones.RegularSequence` supplies the positive counterpart.
  `normalSheafCoordinateMap_injective_of_principal` derives injectivity for `I = (a)` with `a` a
  non-zerodivisor directly from the general regular-sequence theorem,
  `coneToNormalSheaf_isIso_of_principal` upgrades it to a scheme isomorphism, and
  `affineLineOrigin_normalSheafCoordinateMap_injective` / `affineLineOriginIso` instantiate this
  at the origin `(t)` of the affine line `k[t]`.  A genuine length-two case is also constructed:
  `planeRegularGenerators` proves that `t, s` is a weakly regular sequence in `k[s][t]` (the second
  step is verified through the explicit identification `k[s][t]/(t) ≅ k[s]`), and
  `planeOrigin_normalSheafCoordinateMap_injective` / `planeOriginIso` deduce the comparison for
  the origin of the affine plane, so the inductive step of the theorem is exercised with a nonzero
  ideal of previous generators.  Together with the nodal and dual-number counterexamples above
  this shows that the regularity hypothesis is substantive: the same canonical map is an
  isomorphism there and strictly non-injective here.
- `IntersectionTheory.PointChow` constructs the closure-dimension grading, canonical
  rational-equivalence system,
  fundamental cycle, and equivalence `A₀(Spec(k))_ℚ ≃ ℚ`; the fundamental class maps to `1`.
- `VirtualFundamentalClass.ProperPoint` starts from the zero ideal of the identity embedding.  A
  coefficientwise proof identifies its Rees algebra with the constants, so its associated graded
  ring and affine normal cone are constructed and proved isomorphic to `Spec(k)`.  The example
  also constructs Mathlib's affine cotangent presentation of `k → k`, proves both terms zero,
  realizes that very presentation as a cochain complex in degrees `-1` and `0`, and defines the
  derived cotangent object as its image under the derived localization functor.  Its isomorphism
  to zero is derived termwise.  The obstruction theory is the identity of this derived object.
  Perfectness is defined by possession of a genuine finite-free two-term resolution and proved
  by the displayed zero resolution; amplitude is proved by applying every homology functor to the
  derived isomorphism with zero.  The identity chain map is proved to localize to the actual POT
  morphism after the resolution comparison.  The resolved cone's two ranks are defined directly
  as the ranks of those actual
  resolution terms, and only then proved zero; the rank-indexed Gysin map and all cycle and
  Chow transports use those computed ranks rather than independent literal zeros.  The
  cohomological dual complex is constructed by reversing and dualizing these
  terms; its quotient groupoid is proved to have a unique object and a unique arrow between every
  pair, so its stabilizer is computed rather than discarded.  The dual identity POT is proved to
  induce the identity functor on this quotient.
- The point's cone Chow class is then constructed by pushing the actual Rees normal cone's
  fundamental cycle through its isomorphism to the point.  The residue-field extension degree is
  proved to be one, and the resolved zero-Gysin formula gives the ordinary fundamental class and
  virtual degree `1`.  The Rees cone's isomorphism (hence closed immersion) into the affine
  normal sheaf `Spec Sym(I/I²)` is induced by the same canonical degree-one Rees map constructed
  for every ideal; its injectivity for the zero ideal is proved from the vanishing of `I/I²`, and
  its compatibility with the two explicit base identifications is proved on coordinate rings.
  The closed immersion used by the Chow calculation is proved to factor through it.  Sections of
  that affine normal sheaf are identified with objects of the actual dual-complex quotient from
  separately proved uniqueness results.  The point's global intrinsic normal sheaf and cone are
  constructed as zero cone stacks, the POT-induced normal map is the identity closed immersion,
  and both are isomorphic as cone stacks to the actual `[0/0]` obstruction target.  The represented
  affine normal sheaf and Rees cone are globally equivalent to that target, with a 2-cell proving
  compatibility of the Rees immersion.  Finally, the displayed Rees cone satisfies the
  categorical pullback universal property along the rank-zero bundle atlas.  This closes the
  point-specific scheme-level path and its zero global cone-stack model.  It does not supply the
  missing general global-stack Picard/cone construction or general resolution independence.

The point-specific scheme-level equality is fully constructed, but it does not discharge the
remaining general scheme/stack Chow, Gysin, cone, independence, and functoriality tasks above.
